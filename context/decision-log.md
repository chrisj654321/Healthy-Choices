# Decision log

_Running record: decision → why → outcome. Newest first. Add an entry for every consequential decision; fill in outcomes when they land._

| Date | Decision | Why | Outcome |
|---|---|---|---|
| 2026-08-19 | **Shipped to production: the scorer form-penalty overhaul + baked-snack tier, best-available "ceiling" display, "$0 today" paywall, first-90 celebration, and 6 new staple products.** Deploy = OTA (`eas update --channel production`) for the JS + `upload-products-db.js` + `publish-manifest.js` for the re-graded catalog (dbVersion 2026-08-17-1). **Scorer final state:** fried snacks cap 50, baked/popped/puffed analogues cap 65 (`BAKED_SNACK_CAP`), simple whole-kernel popcorn exempt (scores on merits), processed/cured meat cap 40, refined-grain #1-ingredient −25, frying oils neutral; Rule 3 now reads a compound first ingredient's true primary (fixed the Caulipower cauliflower-crust false positive, 40→65). Baked tier came from Chad's Option-C research recommendation (`context/research/baked-vs-fried-chips.md`, 623 tests). Reviewer tightened the baked brand allowlist to Popchips only — Way Better tortilla chips default to fried (50). Catalog re-download triggers off the published **manifest** (productStore.js prefers remote manifest over the hardcoded `DB_VERSION` fallback), so existing installs pull new scores within the 300s CDN window without needing a build. **Avocado-oil research (Chad, 9 peer-reviewed sources) reviewed → NO scoring penalty added** — evidence doesn't support penalizing "avocado oil" in an ingredient list; the shipped "oils neutral" treatment is the correct middle. Share cards shipped in the OTA but are DORMANT (need `react-native-view-shot`/`expo-sharing` in the next EAS build; lazy-required so no crash — share falls back to text until then). | Founder: "push it live." The scorer accuracy work was the keystone (a wrong score is worst when we surface sub-80 "best available" picks); the baked/avocado questions were resolved by Chad research before shipping so the catalog only re-grades once. | LIVE (2026-08-19). Commits a8358d8 (scorer+alternatives), 3f94c1b (paywall), b2458ba (first-90+share), ba58619 (6 staples), f670b86 (manifest bump), a9986ac (avocado research), e2b5bb3 (baked tier). **Watch Sentry** for any OTA crash (esp. the lazy share-require path, though guarded). **Still queued:** share cards activate on next EAS build; kettle-corn 90 flags a possibly-light added-sugar weighting (future tune); Fix & Fogg + Maple Hill parked pending company records; favorites, guest-mode/late-signin, per-goal-diet-note rewire, the "$0 today" already shipped. Chad's 893-ingredient-queue still open; codex CLI broken (missing `codex-code-mode-host.exe`) so Chad runs via ChatGPT paste for now. | 
| 2026-08-17 | **Scorer form-penalty model APPROVED — the scorer will penalize a food's FORM, not just read its ingredient list.** Accuracy audit against the live catalog (`assets/db/products.db`) found the scorer rewards short clean-looking labels and ignores whether the food is fried/refined/processed: **Lay's Classic 91, Fritos 91, Hebrew National franks 96, Bob Evans pork sausage 96, Applegate deli turkey 96, Boulder Canyon "avocado oil" chips 96** — and 42% of the catalog scores 80+, 31% scores 90+ (too generous). Four rules approved: (1) **processed/cured meat → hard cap 40** (trigger: nitrate/nitrite or cured-meat name — hot dog/frank/sausage/bacon/salami/pepperoni/bologna/deli-luncheon/ham/jerky; WHO Group 1 carcinogen basis); (2) **fried snack → hard cap 50** (trigger: fried-form signal — potato/corn/tortilla chips, kettle, fried, crisps/puffs); (3) **refined-grain base (#1 ingredient enriched/white flour or white rice flour) → −25 soft penalty** (deliberately a penalty not a cap, so a mostly-good product isn't nuked); (4) **snack/frying oils (avocado, canola, sunflower, vegetable oil) → neutral, never a positive** — remove avocado oil from the whole-food credit list at `scorer.js:310`, so a fried chip can't be rescued by a claimed (often adulterated — see avocado-oil brief) frying oil. Caps applied as `min(score, cap)`. **Deliberate accuracy safeguard: the rules are form/ingredient-aware, NOT blunt category caps** — Simple Mills almond-flour crackers (93) and Mary's Gone whole-grain crackers (94) stay high because they are neither fried nor refined-grain; whole proteins (fresh chicken, salmon, ground beef) are not processed meat so the cap never touches them. | Founder made grading accuracy the keystone once he chose to surface sub-80 "best available" products — a wrong score misleads exactly when people trust us. He caught the real flaw himself ("chips are gonna be fried... deli meat is a class one carcinogen... should be impossible to get an 80"). Cap-vs-penalty split and the safeguard exist so the fix doesn't create a NEW inaccuracy (blanket-capping good crackers). Numbers are constants, explicitly tunable after the re-grade. | APPROVED 2026-08-17, not yet built. Assigned to Hadrian as its NEXT task after the two UI changes it is currently building (same working tree, avoid collision). Hadrian must produce a full before/after score-drift report on all 1,092 products (same discipline as the 2026-08-08/08-11 ingredient-audit drifts) for review before any commit. Must land BEFORE the "best-available" ceiling display goes live. Expected movers: Lay's/Fritos/Boulder Canyon 91-96→50, franks/sausage/deli meat 96→40; safeguard check = Simple Mills 93 and Mary's Gone 94 must NOT move. | 
| 2026-08-17 | **Competitor audit → four linked workstreams kicked off: scorer accuracy audit, honest best-available display for empty categories, remove letter grades from the UI, and a staple-category product wave.** Started as competitor research (7 direct food scanners — Yuka ~80M users/$14.99yr, Bobby Approved, Fig, Open Food Facts, EWG, Fooducate, GoCoCo; artifact "The Scanner Wars" at claude.ai/code/artifact/4ba997cb-98d3-4cfe-8564-0781d28716f4). Key market finding: NO big scanner shows parent-company ownership or political money — but new single-purpose apps (WalletVote=political only, Scan Owner=ownership only, EXPOSR=additives+ownership) have started attacking that lane, so the money-trail moat is contested but still fragmented. **Correction mid-session:** a first-pass "features to add" list (alternatives, search, diet/allergen alerts) was generated from the competitor template WITHOUT reading the app — all already shipped (`utils/alternatives.js` Pro-gated, `ProductSearchScreen`, `ScanHistoryScreen`, allergen banners in `ProductScoreScreen`). Real gaps are small: favorites (missing), per-goal diet-note rewire (goalNote keyed to OLD goal ids, no-ops for new PRIMARY_GOAL_OPTIONS), and catalog depth. **Catalog audit against the real shipping DB (`assets/db/products.db`, script in scratchpad):** 1,092 products, 385 score 80+, 33 categories, 17 under 12. Gap to fill everything to 12 = ~130, but ~77 of that is in categories that SHOULD stay sparse (chips=fried, crackers=refined, deli meat=WHO Group 1 carcinogen) — so the real fillable gap is ~50 in staple aisles where clean products exist. **Founder decisions:** (1) never use letter grades again — the bar is 80+/100 (`scoreToGrade` maps 90→A/80→B, comment says letter is "retained only for color"); (2) empty/low categories show the single best-available product labeled honestly ("Highest-scored X we found — still only N/100"), NOT nothing; (3) stock staple aisles even below 80 so they aren't empty; (4) grading accuracy is the keystone — the moment we surface a sub-80 as "best," the score IS the product; (5) route product research to Chad; (6) add the avocado-oil-adulteration finding to the app but verify from primary sources first. | Founder asked for competitor research, then steered it into a product-strategy session. He caught the untested-recommendation error himself ("do you just not know what our app offers?"), which is why the catalog audit was run against real data before any further recommendation. Letter grades removed because he finds them misleading and the app is already moving off them. Accuracy elevated to keystone because Option B (show best-available) makes a wrong score actively misleading. | KICKED OFF 2026-08-17, nothing shipped yet. Chad board: two briefs drafted — staple-category product wave (curated shortlist: coffee creamer, bread, snack bars, granola + top-ups; granola needs no-added-sugar to clear 80) and avocado-oil verification (anchor UC Davis 2020 *Food Control*; gates a future scoring signal + copy, ships to no one until verified + Opus-reviewed). Priorities.md P0 block added (4 workstreams, sequenced: accuracy audit → display + letter removal → product wave). Memory saved: `feedback-no-letter-grades`, `competitor-money-trail-entrants`. **Still to do:** build the accuracy reference set + run it; Hadrian specs for the display change (relax alternatives query to best-in-category fallback; current trigger only fires on D/F at `ProductScoreScreen.js:518`) and the letter-grade removal (GradeRing/scoreToVerdict/haptics all grade-keyed). | 
| 2026-08-16 | **Apple Ads (Apple Search Ads) zero-spend diagnosis + 4-ad-group keyword ship-list; Apple Ads API deferred.** Campaign "Food Expose: Barcode Scanner" showed $0 spend / 0 impressions across all 7 keywords, including valid conquest terms (yuka, bobby approved). Ruled OUT billing (valid Mastercard …8481, Orlando US address, promo credit applied 2026-08-15). Root cause = brand-new account (≤1 day old) still in **business verification / first-time review**, plus $1.20 bids likely under the auction floor for hot conquest terms. Keyword plan built two ways (Claude web-research + Chad/gpt-5.6 via `codex exec`) and reconciled: (A) Conquest EXACT ~$2 — yuka, fooducate, codecheck, open food facts, bobby approved, flavcity, plus qualified `fig food scanner`/`olive food scanner`/`trash panda food scanner`/`ewg healthy living` (bare single words like [fig]/[olive] pull fruit traffic even on exact); ADDED trash panda (top-5 competitor was missing). (B) Category — EXACT control group ~$1.50 + a separate BROAD + Search Match discovery group. (C) Long-tail intent EXACT ~$1. (D) Ownership moat EXACT ~$0.80 — "who owns this brand"/"who owns my food"/"food company ownership" (uncontested, cheap, perfectly qualified — the money-trail differentiator). Structural fixes: drop the "23–50 female" demographic filter to All; delete typo [exposr food]; verify [oasis] is really a food app. **API deferred:** Apple requires a SEPARATE Apple ID for the API user (a sole Account Admin cannot self-assign an API role, and the admin's API page has no create-client button), and the API only reads the same keyword numbers the dashboard already shows — no value at $0 spend. EC P-256 key pair pre-generated and saved at `C:\Users\chris\.apple-ads\` (private key kept OUT of git) for reuse when the 2nd Apple ID is set up later. | Founder started paid Apple Ads, got $0 spend, and wanted API-based keyword tracking; needed to know why nothing spent and which keywords to run. Two-model reconciliation raised confidence — both independently flagged verification + bids (not keyword choice) as the zero-spend cause. API path researched to a hard stop (2nd Apple ID) that adds nothing at this stage. | PENDING (2026-08-16): founder submitting business verification and building new campaigns from the ship-list. Revisit once verification clears — confirm ads serve, then judge whether conquest bids need raising toward ~$2. API to be revisited only when spend + automation justify a 2nd Apple ID. |
| 2026-08-11 | **Applied the 93 reviewed score changes from the ingredient-audit sync into the two score stores (`INGREDIENT_DB` in `ingredients.js` + `CACHED_INGREDIENT_ANALYSIS` in `ingredientCache.js`).** Chad's audit produced 100 `proposed` rows; the 7 the reviewer marked `FIX` were held. A deterministic Node script (`scripts/merge-score-changes.js`) did the patch — dry-run diff reviewed first, then `--apply`; it patches only the `risk`/`flag` value tokens and preserves formatting byte-for-byte. Two founder decisions on 4 conflict rows: `green s` kept at `moderate` (risk 7 — the cache has no `caution` tier), and the 3 wheat rows (`seitan`/`semolina`/`semolina wheat`) applied as risk 3/`ok` (the CSV's `allergen` word ignored — it is not a risk flag). **Key review finding: the CSV baseline was stale** — the 2026-08-09 catalog rebuild had already moved on-disk values after Chad's mid-July review, so of the 93: 35 were true no-ops, ~43 nudged risk without crossing a flag threshold, and only 13 actually flipped a user-visible flag — every one a SOFTENING (caution/moderate → ok): MSG, natural flavors/flavoring, calcium disodium EDTA, saccharin, modified food starch, datem, disodium guanylate/inosinate, calcium peroxide, dextrin, SAIB. The old flags did not match the risk value; the softenings fit the neutral-regulatory-framing / no-fearmongering doctrine. Founder gave explicit conscious sign-off on MSG→ok and natural flavors→ok (the two highest-visibility flips) | Chad's ingredient-audit sync had 60 held score changes resolved; the reviewed batch needed to reach the live scorer. Script-first (deterministic) + Opus review of the flag-flip subset caught that the CSV's own `currentRisk` column misrepresented the real effect | Committed `6766f0c` (by explicit path — three files only, left unrelated working-tree changes alone). 502/502 tests pass (incl. `scorer.test.js` + Rules-of-Hooks lint gate). Both files Babel-parse OK; duplicate-cache-key last-wins bug caught and handled by the script (patches the runtime-winning last occurrence). These stores are bundled JS, not `products.db`, so they reach users on the next `eas update` OTA (not the db-upload gate). **Still queued from the same audit:** 893 `imported` loose matches for careful review; the 7 `FIX` rows for a second look |
| 2026-08-09 | **FIRST production OTA update ever shipped — hot-fixed a live sev-1 crash on the App Store without an Apple build.** 1.2.0 was approved and went live on the App Store (founder confirmed). It carries the back-button change `010fac4` (2026-08-08) that put three hooks (`tabBarStuck`/`tabBarYRef`/`stuckRef`) BELOW the `if (!product) return null` / `if (!result)` early returns in `ProductScoreScreen.js` — a Rules-of-Hooks violation that crashed with "rendered more hooks than during the previous render" the instant a product's score finished computing. Every user tapping any product got a dead loading screen (Specs visible, stuck). Root cause fixed in `fcdbf1c` (hooks moved above the early returns; verified every hook now precedes both returns). Because the live build is 1.2.0 — the first binary carrying `expo-updates` — the JS-only fix was pushed via `eas update --channel production --platform ios` instead of a new submission. Update group `200a3477-4068-479b-8e60-cd89090b35b3`, iOS update `019ff35c-a857-7c40-9f48-bd4f3f25af7b`, runtime 1.2.0, from commit `22f6156`. Bundle smoke-checked with `expo export` (clean 8.6MB Hermes bundle) before publishing. **The OTA carries the full committed HEAD, not just the one-line fix** — so this same push also delivered this session's back-button redesign, egg-scoring retier, the 89 ingredient score changes, 531 aliases, and 13 researched entries to live users (all tested, 502/502; Apple explicitly permits bug-fix/JS OTAs). Founder explicitly authorized Claude to run `eas update` (not a metered build/submit, so outside the "founder-only" EAS rule); Claude ran it after confirming `eas whoami`, the production channel mapping, and a clean `src/` tree | Live crash on every product tap; the OTA lane built across Phases 0-3 this session existed precisely for this — a JS crash that would otherwise wait weeks for Apple re-review | Published successfully. **Phase 0 is now DONE** — 1.2.0 live means the update engine is in users' hands and the whole decouple-from-Apple architecture is active. Verification: Sentry filtered on `ota.update_id = 019ff35c…` should show the hooks error stop (the `ota.*` tagging added 2026-07-26 exists for exactly this). **Now-unblocked (was gated on "after 1.2.0 approved"):** upload the rebuilt `products.db` (`node scripts/upload-products-db.js`) + bump `dbVersion` in `scripts/manifest.json` + `publish-manifest.js`, to ship the egg-scoring + ingredient-audit catalog to devices; and the Vital Farms 100-score screenshot recapture |
| 2026-08-08 | **The 26 still-novel queue tokens handled by CATEGORY, not force-fitted into 26 entries — 13 real entries added, each risk tied to a primary regulatory source (real web research, not memory).** Of the 26: **7 genuine substances** researched against FDA/eCFR/EFSA and added — ferric phosphate (Low, iron fortificant, 21 CFR 184.1301), ferric pyrophosphate (Low, 21 CFR 184.1304), distilled monoglyceride (Low, emulsifier, 21 CFR 184.1505), mixed triglycerides (Low, food fat, 21 CFR 172.861), disodium pyrophosphate/SAPP (Medium, phosphate leavening acid, 21 CFR 182.1087), ammonium phosphate (Medium, 21 CFR 184.1141a), natural smoke flavor (Medium, PAH-regulated EC 2065/2003 — but this one was already an existing key resolving Low, so it was SKIPPED rather than duplicated/overridden). **6 FD&C dye fragments** ("fd c red no", "artificial color fd c red", etc.) added — the four RED-specific ones as High (FD&C Red 40/Red 3 are both High; Red 3 FDA-banned 2025), the generic "fd c colors" as Medium. **2 whole foods** added — "orange puree" (Low, fruit — this also fixes the alias pass's deliberate exclusion, where bare "orange" fuzzy-hit an orange DYE and returned High) and "gluten free flour" (Low, refined non-wheat flour). **The remaining 11 tokens were deliberately NOT given entries** — they are label boilerplate ("preserves freshness", "to retain freshness", "maintains freshness", "non gmo", "gluten free", "granular"), generic class phrases ("leavening agents"), or OCR junk ("slat", "ntss", "including smoke", "processed with natural smoke flavor"). Creating risk entries for non-ingredients would be inventing data; confirmed first that the parser already treats them as unknown AND the scorer never penalizes an unknown (scorer.js line 818: "never penalized for a data gap"), so they are harmless as-is. Each researched risk maps to the app's 2/5/8 enum by matching the flag the regulatory basis implies (iron fortificants/fats/emulsifiers Low; phosphate additives and PAH-bearing smoke Medium; synthetic reds High). Every entry carries its citation as an in-file `// src:` comment for auditability | Founder: "do the 26 new substances" — after being told the alias pass left 26 genuinely-new tokens that needed real sourced research, not memory-generation. This is Chad's evidence-review job, done the same standard while Chad is rate-limited | Committed. `scripts/ingredient-audit/add-researched.js` holds the entries + source list. 502/502 tests pass; file parses; all 13 resolve via the matcher (spot-checked: ferric phosphate→Low, fd c red no→High, orange puree→Low, disodium pyrophosphate→Medium). Catalog rebuilt, `ingredient_analysis` now 4,100 rows. **Score drift on the 1,092 curated products: 0** — these tokens live in the broader scanned corpus (where the queue frequencies came from), not the curated catalog, so this is pure coverage for live scans of arbitrary products with zero change to any curated score. Ships on the same post-1.2.0 gate |
| 2026-08-08 | **Ingredient-queue alias pass — 531 messy label strings mapped onto ingredients the dictionary already explains, WITHOUT generating any new facts. Claude did the work Chad normally does (Chad is rate-limited).** First measured the real problem: ran all 2,250 queue tokens through the app's OWN shipped matcher (`lookupIngredient` via the same VM-bundle the SQLite build uses) — 1,693 (75%) already resolve at scan time, only 557 were genuinely unmatched. Of those 557: 531 are aliases (a messy label variant of a known ingredient — `semi sweet chocolate`→chocolate, `orange juice solids`→orange juice, `crushed garlic`→garlic) and 26 are truly-new substances left for real sourced research (`ferric phosphate`, `distilled monoglyceride`, dye fragments like `fd c red no`, plus OCR junk `slat`/`ntss`). **Safety model — no fabrication:** an alias is created ONLY when the app's own matcher resolves a deterministically-cleaned sub-phrase of the token; the new entry copies the matched parent's risk/category/explanation verbatim; nothing is invented. Built `scripts/ingredient-audit/triage-queue.js` (buckets the queue) and `build-aliases.js` (generates every contiguous sub-phrase of the noise-stripped token, longest-first and right-to-left so the English head noun wins, tests each against the matcher, writes a `alias-map.csv` review file). **Six real precision bugs found and fixed by auditing the output, not by trusting the first pass:** (1) `contains` matched 14 tokens as label boilerplate → denylisted; (2) `d alpha tocopheryl acetate`→`acetate` (vitamin E mis-mapped to a generic ion) → GENERIC_DENY of bare ions/chemistry fragments; (3) form-words `swirl`/`crumbs`/`pieces`/`root` beating the real ingredient (`almond pieces`→pieces) → denylisted cuts/shapes/containers so the modifier wins; (4) `orange puree`/`orange juice solids`→`orange`→**High** because bare "orange" fuzzy-hits an orange DYE → refuse any single-word candidate that resolves High (a whole label reducing to one scary word is almost always a dye false-positive) + generate sub-phrases so `orange juice` is tried before `orange`; (5) `high fructose corn syrup`→`corn` because HFCS resolves to risk **9** from INGREDIENT_DB and the code only knew the 2/5/8 enum → threshold any risk via the same `>=8/>=5` rule `riskToFlag()` uses, and pull text from the DB entry's `note` field (cache entries use `explanation`); (6) leading qualifiers (`vegetable mono diglycerides`→`vegetable`) → right-to-left scan within each phrase length. Final High maps (12) are all trustworthy multi-word matches (hydrogenated oils, sodium benzoate, sodium nitrite, HFCS, TBHQ); zero blank explanations | Founder: "chad is actually rate limited, but can you do that work?" — after confirming Chad's audit ships without Claude re-review. Answer given honestly: the alias half is safe for Claude (string→known-key, no fact generation); the ~26 genuinely-new substances need real sourced research and were NOT mass-generated from memory | Applied to `ingredientCache.js` (531 alias entries, file parses, 502/502 tests pass). Catalog rebuilt — `ingredient_analysis` now 4,087 rows (was 3,556). **Score drift: 21 of 1,092 products moved (5 up, 16 down).** Every downward move verified as previously-INVISIBLE processed ingredients now correctly recognized — biggest, Campbell's Minestrone -25, traced to two `italian sausage (pork...bha/bht...)` tokens going UNKNOWN→Medium (before/after `lookupIngredient` diff against HEAD confirmed it, not a mis-map). Ships through the Phase-3 lane on the same post-1.2.0-approval gate. Review file `scripts/ingredient-audit/alias-map.csv`; research bucket `still-novel.json` (26 items) |
| 2026-08-08 | **ChatGPT (Chad) ingredient audit worked into the app — 28 new ingredients + 89 score changes applied to `ingredientCache.js`, WITHOUT a Claude re-review.** Founder's explicit call: Chad runs the same never-fabricate / regulatory-facts rails, has his own independent reviewer, and gpt-5.5 is a peer model — so re-reviewing 150 already-reviewed items is duplicated work with no payoff. Reduced the earlier 5-phase plan (which had a full Opus verification phase) to the mechanical path: import → apply → drift report → ship. Two rules kept, both stated to the founder and NOT a form of re-review: (1) only the 90 proposals Chad marked `proposed` were applied; the 60 he marked `needs-more-research` are his OWN reviewer's "not finished" flag and are held — honoring his status field, not overriding it. (2) The audit uses a finer 2-9 risk scale but the app's `IngredientRisk` enum is only 2/5/8, so each raw score maps to the enum step whose display flag matches — the exact `>=8 avoid / >=5 moderate / else ok` thresholds the app's own `riskToFlag()` uses (so raw 3/4→Low, 7→Medium, 9→High). Built `scripts/ingredient-audit/import-audit.js`: real RFC-4180 CSV parser, `--dry-run`, targeted single-line rewrites that preserve the 47 interspersed comments and every untouched entry byte-for-byte (never regenerates the file). Of the 90 `proposed`: 89 applied (4 raises — all obscure EU dyes brown ht / patent blue v / quinoline yellow ws + melatonin, none in the catalog; 85 lowers — GRAS additives like EDTA/calcium salts corrected down), 1 no-op after mapping. **Score-drift report on all 1,092 catalog products: 51 moved, ALL upward (safer), 0 scarier** — consistent with 85/89 being over-cautious corrections. Biggest mover Vanilla Yogurt +35 (20→55), traced to three thickeners (modified corn starch, gelatin, modified tapioca starch) each dropping Medium→Low; confirmed legitimate, not a tooling bug. The 60 held items and the 2,250-name `Next Ingredient Queue` are a later standing loop | Founder: "Chad also has the same rules that you have and has a reviewer look at everything... gpt-5.5 is as capable as you are. So we don't need to check it... I just wanna implement it" | Committed. 502/502 tests pass; `ingredientCache.js` parses clean (28 new keys incl. real gaps `fd&c red 3`/`red 40`, `cottonseed oil`, `artificial color`). Catalog rebuilt (`ingredient_analysis` now 3,556 rows). **Ships through the Phase-3 lane on the same gate as the pending catalog — upload + `dbVersion` bump AFTER 1.2.0 is approved**, so existing installs get the overlay-capable binary first. Plan doc: `context/plan-ingredient-audit-integration.md` |
| 2026-08-08 | **Phase 3 built as an OVERLAY, not a replacement — the bundled `companies.js` / `ingredientCache.js` stay put as the offline seed and the catalog's copy is assigned on top at startup.** The blocking constraint, found before writing any code: `scorer.js` imports `COMPANY_DB` and `CACHED_INGREDIENT_ANALYSIS` through plain synchronous imports and `scoreProduct()` is called synchronously from the render path (`ProductScoreScreen.js:321`). Replacing those imports with awaited SQLite reads would have meant making the scorer async — directly against the plan's own guardrail that the scorer stays on-device and synchronous. So `src/data/remoteDataOverlay.js` `Object.assign`s catalog rows onto the already-exported objects; Metro hands every importer the same object reference, so one assign updates `scorer.js`, `productParser.js`, `spotlight.js` and four screens **with zero changes to any consumer**. Catalog gains `companies`, `brand_company_map` (both brand lookups in one table, separated by a `kind` column, since a brand can legitimately appear in both `BRAND_TO_COMPANY` and `BRAND_PARENT_MAP` — confirmed: 'doritos' is in both), and `ingredient_analysis`; schemaVersion 1→2. `CACHED_INGREDIENT_ANALYSIS` needed a sandbox loader rather than `evalObjectLiteral` because its values reference the `IngredientRisk` enum declared above it. **Three deliberate properties**: (1) **additive only** — a company absent from the catalog keeps its bundled record rather than vanishing, because a stale record beats a blank screen on a transparency product; removals still need an app release. (2) **never throws**, same contract as `productStore.js`. (3) **tolerates an old catalog** — probes `sqlite_master` first so a pre-schemaVersion-2 `products.db` is a normal state, not a Sentry event. **Known accepted seam**: `App.js:93` fires `initProductStore()` without awaiting, so a screen already mounted at cold start can render one frame from bundled data before the overlay lands. Every company surface is reached by navigation after startup, so in practice they read post-overlay; the alternative was blocking first paint on a database read, which is worse. Validator hardened in the same pass — it now fails if any of the three tables is missing, if the company count doesn't match source, if both brand kinds aren't present, or if any company row isn't valid JSON, because a build that silently dropped the tables would otherwise still pass every existing check AND still run, just permanently on bundled data — the exact failure this phase exists to end | Founder: "phase 3 go." Last structural piece of the decouple-from-Apple plan; the biggest win for the sourcing-transparency pipeline, whose output was previously stuck behind an Apple build | Committed `0b85827`. 502/502 tests pass (7 new for the overlay: null handle, all-tables, additive-not-destructive, old-catalog, partial-tables, malformed-rows, query-failure). Validation PASS. Catalog rebuilt: 2.64 MB (was 1.01 MB), 302 companies + 1,554 brand rows + 3,528 ingredient analyses. **The rebuild also picks up two already-committed fixes that had never reached a built catalog** — the egg housing-tier rescore (Vital Farms pasture-raised now 100, above organic 89 and free-range 88, which is what the App Store screenshot claims) and the Kroger Lunchables image. **Not yet uploaded** — publishing the new `products.db` changes what every live install sees, so it waits on explicit founder approval |
| 2026-08-08 | **Phase 2's manifest published to Supabase Storage — the remote-config lane is now live end to end.** Phase 2 shipped the app-side reader (`src/utils/remoteConfig.js`) on 2026-08-06, but `Catalog/manifest.json` was never created in the bucket, so every launch got a 400 and `getRemoteDbConfig()` silently fell back to the hardcoded `DB_VERSION`/`REMOTE_DB_URL` constants. Verified the 400 before acting, and verified HTTP 200 + `application/json` after. **Decision: the manifest's source of truth is a committed file (`scripts/manifest.json`) published by a script (`scripts/publish-manifest.js`), NOT a Supabase dashboard edit.** A dashboard edit leaves no git trace, no review, and no way to see what changed — the same objection that made Phase 1's paywall a hybrid rather than fully dashboard-driven. The script re-implements the device's own validation rules (non-empty `dbVersion`, `dbUrl` inside `EXPECTED_DB_URL_PREFIX`, `config` an object) so a manifest the app would silently reject fails locally instead of publishing; `--dry-run` validates without uploading. CDN `cacheControl` set to 300s deliberately (Supabase defaults to 3600s) — the manifest is the one file that must take effect quickly, since it carries the kill switches. First published values match the hardcoded constants exactly, so publishing was behaviour-neutral by construction | The manifest was the last blocker on Phase 2 being real rather than nominal, and it gates Phase 3 (which reads catalog location from it). Founder approved the upload explicitly — publishing a public file that live installs read is outward-facing, not a routine write | Live and verified (HTTP 200). Committed `fe6cc09`. **Phase 2 is now genuinely complete** — it was marked done on 2026-08-06 while the lane was still dead. A rebuilt `products.db` can now reach devices by uploading the file and editing one line in `scripts/manifest.json`, with no Apple build and no OTA. **Still gated by Phase 0**: existing installs only benefit once a binary carrying this reader is actually approved — 1.2.0 is that binary and is still rejected/unsubmitted |
| 2026-08-06 | **Program started to decouple releases from Apple review — 4 phases, plan at [plan-decouple-from-apple.md](plan-decouple-from-apple.md).** Principle: EAS/Apple builds carry native changes ONLY; all JS ships OTA (`expo-updates`), all data ships from Supabase. Two verified findings reshaped the plan: (1) **`react-native-purchases-ui` has been in `package.json` since 2026-06-06 — before the first approval (2026-07-06) — and is already called live in `ProfileScreen.js` (`presentCustomerCenter()`), so the RevenueCat UI native module is present in every shipped binary**, meaning RC paywall surfaces are reachable from JS with no new build. (2) **RC *multipage* remote paywalls require SDK ≥10.6.0 and we resolve to 10.2.2**, so adopting full RC-rendered paywalls for our two-screen (paywall → exit-offer) flow would itself require a native build — self-defeating. **Architecture decision for Phase 1: hybrid.** Paywall LAYOUT stays in code; only copy/features/product-selection move to the RC Offering `metadata` JSON (dashboard-editable, no build, no OTA). Deliberately rejected making layout dashboard-editable: we were rejected under 3.1.2(c) that same week for price prominence, and a dashboard edit could silently re-break it with no code trace and no Apple re-review. Guardrails set: scorer stays on-device (offline scanning + no-tracking posture); every remote surface needs a bundled safe default; remote content may never assert an offer the store isn't configured to sell; untrusted dashboard/manifest JSON gets per-field validation with per-field fallback | Founder: Apple review turnaround "horribly long"; wants Apple used only for native software, the paywall hosted through RevenueCat, and as much as possible moved off the build cycle | **Phase 0 (resubmit 1.2.0) is a hard prerequisite — OTA can only reach a binary that already ships the update engine, and the first such binary is the currently-rejected 1.2.0.** Phases 1 + 2 delegated to Hadrian in parallel this session (results pending review); Phase 3 (companies/ingredientCache into the downloadable DB) blocked on Phase 2's manifest |
| 2026-08-06 | **Two new company records added — Wayne-Sanderson Farms and Pilgrim's Pride — via the `/political-analysis` pipeline, not sourcing-transparency.** Neither existed in `companies.js` at all (unlike Perdue/Butterball/Foster Farms, which already had complete base records missing only `sourcing`), so this ran the political-analysis 4-agent pipeline (research → fact-check → Opus write → Opus review) to build the base record itself — the sourcing-transparency pipeline can't run on a company that doesn't exist yet. Wayne-Sanderson Farms: verified the full 2022 ownership chain (Cargill + Continental Grain JV bought Sanderson Farms $203/share ~$4.53B, merged with Continental Grain's existing Wayne Farms), correctly kept 3 separate Broiler Chicken Antitrust threads distinct (grower ~$17.75M settlement, a $22.35M 10-defendant consumer settlement where Sanderson's individual share is undisclosed, and an Oct 2023 jury trial Sanderson actually WON — included for balance, not just negatives). Pilgrim's Pride: the headline fact is a real federal criminal conviction (guilty plea to Sherman Act price-fixing, $107,923,572 court-assessed fine, D. Colorado No. 1:20-cr-00330-RM) — technically UNVERIFIED by strict primary-source-access standard (DOJ/CourtListener/SEC all 403'd both research and fact-check passes) but exceptionally corroborated (a CourtListener docket snippet independently matched the exact fine to the dollar, 7 independent outlets converge with zero contradiction, and the company's own 8-K counts as a company-disclosure source under the doctrine's own definition) — reviewer explicitly sustained direct language rather than hedging a real guilty plea as an allegation. **Two real review-stage catches, not rubber-stamps:** (1) Wayne-Sanderson's writer used an FEC figure for `politicalDonations` that turned out to be PAC *receipts* (money raised), not disbursements to candidates — every other company record populates that field with money paid out; reviewer changed it to `null` rather than let a technically-sourced-but-wrong-metric number render as a comparable fact. (2) Pilgrim's Pride's writer initially named all 7 executives who were acquitted/had charges dismissed inside the conviction entry itself — reviewer stripped the names, reasoning that naming people cleared of a crime inside an entry titled "Criminal Price-Fixing Conviction" creates guilt-by-association with zero benefit to the user; kept the outcome as a neutral, unnamed sentence instead. JBS's own separate corporate conduct (FCPA bribery plea, beef price-fixing) was kept entirely out of Pilgrim's Pride's `issues[]`, appearing only as `parentCompany` ownership-structure context — attributing a parent's crimes to its subsidiary would have been a real defamation-adjacent risk | Founder: "Add Sanderson Farms and Pilgrim's Pride too, build them out," after clarifying he wants company coverage built without needing individual products — these two just needed a base record before any sourcing pipeline could reach them | Merged directly into `companies.js`'s `COMPANY_DB` (manual insert — `merge-sourcing.js` only merges the `sourcing` field onto existing records, doesn't create new ones) plus 5 new `BRAND_TO_COMPANY` alias entries (sanderson farms/wayne farms/wayne-sanderson farms → wayne-sanderson; pilgrim's pride/pilgrims pride → pilgrims-pride). 432/432 tests pass. Zero products reference either new companyId — same zero-products pattern as the meat-poultry wave above, both companies reachable via `CompanyListScreen` only for now. **`sourcing` field NOT yet built for these two** — that's a separate future pipeline run, same as it was for Perdue/Butterball/Foster Farms before today |
| 2026-08-06 | **Meat-poultry coverage expanded to 3 more companies (Perdue, Butterball, Foster Farms) with ZERO products added** — founder explicit direction: "I don't need individual products from brands in the database... can we build it without adding individual product." Confirmed technically first (before spending agents): `CompanyListScreen.js` browses `COMPANY_DB` with no product requirement at all, `CompanyProfileScreen.js` renders a "0 products in database" empty state gracefully — so all 3 companies are reachable via company-browse today with zero catalog products referencing them (confirmed post-merge: 0 matching products). Ran the full 6-stage pipeline (Stage 1 CourtListener/SEC script pull incl. Seaboard Corp's 10-Ks for Butterball's JV-parent disclosures → 3 Stage 2 Sonnet researchers → 3 Stage 3 Sonnet fact-checkers → 3 Stage 4 Sonnet writers → 3 Stage 5 Opus reviewers, max-3/max-2 concurrency held throughout). **All three review passes found and fixed real issues, not rubber-stamps**: Perdue — downgraded the Jien wage-fixing settlement's "two independent sources" framing (one of the two, Cohen Milstein, is plaintiffs' own counsel, not independent) and downgraded PVP "No Antibiotics Ever" from `government-record` to `company-disclosure` after finding its sole corroboration was a company PR Newswire release, not a government document; hedged the Chaney PFAS suit's "MDE discovered..." language to "the complaint alleges MDE identified..." (an MTD ruling recites allegations, doesn't find them true). Butterball — rewrote a PETA-FTC-complaint entry that named the FTC inside an `enforcement[]` field (read as agency action even though the prose only said "complaint filed") to lead with "NOT AN ENFORCEMENT ACTION"; removed a phantom recall figure ("5 case-patients/2 states") that fact-check had already ruled unsupported but had crept back into a source-note negation. Foster Farms — the most consequential single catch: "American Humane Certified" was written as a current fact by the researcher, but the fact-checker fetched AHA's own live 101-entry certified-producers directory in full and Foster Farms isn't on it (last documented mention: a Feb 2021 press release) — corrected to historical-through-2021/current-status-disputed rather than an active certification. Also surfaced en route: 3 state/DC-court welfare-adjacent suits CourtListener's federal-only search structurally cannot find (Leining v. Foster Poultry Farms — CA state "Humane Certified" labeling suit, dismissed on preemption 2021; Organic Consumers Association v. Foster Farms — DC Superior Court "five freedoms" claims, pending; ALDF v. Foster Poultry Farms — CA state slaughter-method suit, settled) | Founder, after reviewing the beef grass-finished tier redesign, raised that only 8 meat-poultry companies existed ("there's way more than eight companies... we need to probably try to cover about eighty percent of the market") and specifically flagged poultry as thin, then corrected my assumption that Perdue/Butterball were excluded for bad expected scores — "I assume we don't have those because they probably have bad scores" was wrong, they simply hadn't been added yet, unrelated to quality | Merged via `merge-sourcing.js --dir` (idempotent — correctly skipped all 8 already-merged companies, no `--force` needed). 432/432 tests pass. **No `products.db` rebuild needed** — confirmed 0 products reference any of the 3 new companyIds, so this ships as pure company-browse data, consistent with the founder's explicit zero-products direction. **Still open, flagged not actioned**: Sanderson Farms/Wayne-Sanderson Farms and Pilgrim's Pride have no `companies.js` record at all (unlike these 3, which already had complete records missing only `sourcing`) — a base-record-creation pass is needed before the sourcing pipeline can apply to them; founder said "Add Sanderson Farms and Pilgrim's Pride too, build them out" but this wave didn't reach them |
| 2026-08-05 | **1.2.0 build rejected — Apple 3.1.2(c): paywall showed the calculated monthly price more prominently than the real billed amount.** Confirmed against Apple's actual HIG text ("the sign-up screen displays billing totals... in the most prominent positions. In subordinate positions, it shows breakdowns...") — `PaywallContent.js`'s yearly card showed `$4.17/mo` at 22px bold as the dominant number, with the real `$49.99/year` total in 11px muted text below it, exactly backwards. Fixed on both the main paywall and the exit-offer screen (`OfferContent.js`): billed amount is now the large primary number on every card (period suffix styled smaller so the number itself dominates), calculated per-month price and savings-% are subordinate below. Paired product change: **dropped the 50%-off-first-year intro discount, replaced with a 3-day free trial on yearly only** (monthly unchanged at $7.99/mo, no trial) — `paywallPricing.js`'s `computeOfferPricing` (discount-based) replaced with `computeYearlySavings` (honest yearly-vs-12x-monthly comparison, no strike-through/fake discount math). Trial terms shown per Apple's disclosure rule ("3 Days Free, then $49.99/year") via the already-existing `introOffer()` helper, now wired into both screens instead of just one. Exit-offer screen keeps the same two-page structure the founder wanted — same real offer, reframed around savings-% instead of the main paywall's per-month-breakdown framing, not a second discount. **Still needs founder action outside this repo**: replace the 50%-off intro offer with a 3-day free trial on the yearly product in App Store Connect / RevenueCat — no code change can do this, the store's actual offer configuration must match what the paywall now describes | Founder reviewed the rejection, went through the live paywall himself and diagnosed the same thing Apple flagged, then specified the new pricing (yearly $49.99 + 3-day trial, monthly $7.99 unchanged, drop the discount) and confirmed the two-screen reframe approach when asked about the exit-offer screen's fate | 432/432 tests pass, committed (`4c0d817`). **ASC config updated same day** — founder added the 3-day trial to yearly and, going further than planned, also stripped monthly's discount, so both plans now run at full undiscounted price ($49.99/yr + 3-day trial, $7.99/mo flat). Deliberate: current subscriber count is tiny, minimal risk of upsetting anyone, price treated as adjustable later (see memory/claude-memory.md 2026-08-05 entry) |
| 2026-08-03 | **Resolved all three pre-UI blockers Stage 5 flagged for meat-poultry (2026-07-30), logged not fixed at the time.** (A) Populated `amount` on every enforcement entry where the prose already stated a specific, multi-outlet-corroborated figure -- Tyson's cattle-beef entry actually held two distinct settlements ($82.5M direct-purchaser, $55M consumer) collapsed into one null-amount field; split into two entries so neither figure got lost. (B) Added a dedicated `recalls[]` array to evidence-schema.md with a `scale` field ('own-facility' vs. 'third-party-co-packer' vs. 'unknown') -- the actual defect: Dietz & Watson's co-packer cheese recall and Boar's Head's 10-death listeria outbreak had been carrying the identical enforcement badge. Moved 13 recall entries out of enforcement[]/practices[] across all 8 companies; Boar's Head's now carries real CDC health-impact data (61 illnesses, 10 deaths) in a structured `healthImpact` field instead of buried in prose. (C) Re-ran GAP step for all 8 companies against one consistent 3-method check (Manufacturers directory, Shoppers directory, site-restricted search) instead of whichever page a researcher happened to open -- found a real GAP Step 1 relationship for Tyson via its "Open Prairie Natural Meats" sub-brand (correctly kept the company-wide field at 'unknown' rather than overclaiming), resolving an old disputed secondary-source claim with first-party evidence. 3 companies (Kraft Heinz, Johnsonville, Smithfield) moved from 'unknown' to a rigorously-confirmed 'none' | Founder: "continue with meat-poultry and seafood pipeline" -- these were real, identified defects sitting in already-merged, already-shipped data; fixing them while the pipeline context was still fresh beat revisiting cold later | `merge-sourcing.js` gained a `--force` flag (previously insert-only, skipped anything already merged -- had to be fixed before any of this could re-merge). All 8 companies' `_final.js` checkpoints and `companies.js` updated. None of these changes affect scoring (`scorer.js` only reads `welfareMeatPoultry` fields) -- no `products.db` rebuild needed. 431/431 tests passing |
| 2026-07-31 | **Housing-imagery policy revised: real photos of identifiable/advocacy-sourced facilities are fine, as long as the app never discloses whose facility is shown.** Reverses part of the "never depict a specific company's facility" rule from the same day's earlier entry. Founder pushed back twice: the app's tier photos already carry a generic "similar to this" caption regardless of source, so a photo depicting a real, identifiable, even advocacy-photographed facility isn't a misrepresentation as long as it isn't disclosed/captioned as belonging to any company — "unless a photo is modified to look worse than it actually is, it's probably a fairly accurate depiction... nothing to shy away from." Reviewed the actual photo before swapping it in (not just the source): an HSUS-photographed Smithfield gestation-crate barn (CC BY 3.0, explicit release confirmed via Wikimedia's permissions process) — bright natural lighting, no visible injury/blood, ordinary documentary framing, not dramatized. `housing-imagery.md` rewritten: the "avoid advocacy/investigation photography" rule now applies to the SPECIFIC photo's tone, not a blanket rule against the source category; the "never a specific company's facility" rule now scoped explicitly to AI GENERATION only (fabricating an image and implying it's a real company's facility invents evidence; a real photo depicts something that actually happened) | Founder: "you created that rule not me... similar living conditions is fine to depict, we don't have to disclose whose facility the photo is" — correct that the original rule conflated two separable concerns (photo tone vs. company disclosure) and applied the stricter one by default without being asked to | `assets/sourcing/meat-poultry/gestation-crate.png` (AI-generated placeholder) replaced with `gestation-crate.jpg` (the real HSUS/Smithfield photo). `assets/sourcing/manifest.json` and `housing-imagery.md` updated to record the policy and reasoning for future modules (dairy, produce-farms) to follow |
| 2026-07-31 | **Egg housing-tier space figures unified to square feet** — the conventional/caged tier's generic standard was the only one displaying in square INCHES (UEP's own published "67 sq in/hen"), while every other tier (cage-free, free-range, pasture-raised) displays sq ft, making a side-by-side comparison require the reader to do unit conversion themselves. Converted to 0.47 sq ft/hen (67÷144, rounded to 2 decimals), original sourced figure preserved in a code comment for traceability | Founder: "for the chicken, it says square inches. That's weird... everything to say square feet because that's a quantifiable dimension for most people" | `sourcingMatch.js`'s `GENERIC_TIER_STANDARDS.conventional` and both dependent tests updated. Display-text-only change — not used in score calculation, so no `products.db` rebuild needed. 431/431 tests passing |
| 2026-07-31 | **Housing-tier photos sourced for meat-poultry and seafood** (prep for a future card, same as eggs — data-only, no UI built yet). 6 tiers identified from what's actually scored: beef (grass-finished, conventional/feedlot), pork (crate-free, gestation-crate), seafood (wild-caught, farmed). 5 of 6 found as real, license-verified, non-advocacy, non-company-specific photos (USDA/NOAA public domain + Wikimedia CC): grass-fed beef (USDA/Ryan Thompson), feedlot beef (Wikimedia, Texas Panhandle), free-range/crate-free pork (Wikimedia, Germany), wild-caught seafood (NOAA, Chilean purse-seine catch), farmed shrimp (Wikimedia, Madura Indonesia). Only the gestation-crate tier needed AI generation — the one real candidate found on Wikimedia depicted a **named Smithfield Foods/Murphy Brown facility**, failing two hard rules simultaneously (never a specific company's facility; avoid advocacy-photography sourcing — this really was advocacy content, same pattern as eggs' "Battery cages" category). Prompt confirmed with the founder before submitting (standing rule), generated via Nano Banana Pro (Higgsfield), AI-provenance XMP metadata embedded per the settled two-layer disclosure policy — the existing blanket ToS line was already generic enough to cover this without editing. All 6 images resized/compressed (2-3MB raw down to 64-431KB) to match the eggs precedent before shipping as app assets | Founder: "source photos for the housing tiers," after directing the meat-poultry/seafood scoring build itself | `assets/sourcing/meat-poultry/` (grass-finished-beef.jpg, conventional-beef-feedlot.jpg, crate-free-pork.jpg, gestation-crate.png) and `assets/sourcing/seafood/` (wild-caught.jpg, farmed.jpg) populated; `assets/sourcing/manifest.json` documents license/author/source for every real photo and the generation details for the one AI image. Data/imagery only — the actual UI card (a meat-poultry/seafood equivalent of the egg Living Conditions card) is still a separate future build, per standing pipeline sequencing |
| 2026-07-30 | **Three pre-UI blockers flagged by the meat-poultry Stage 5 review, logged not fixed — none block the just-completed data merge (nothing renders `sourcing` yet), but each will distort the app's computed comparisons the day a meat-poultry Living Conditions-style card ships.** (A) `amount` population is asymmetric across the batch: Tyson has ~$300M in settlements stated in prose but `amount: null` on every entry (Smithfield's are populated), while small deli brands (Land O'Frost $13,494+$44,262, Dietz & Watson $12,340, Boar's Head $2,100+$3.1M) are populated — any future UI that sums or displays penalty totals would show the big antitrust settlers at zero and the small brands "with money against them." Fix: populate `amount` wherever prose states a specific multi-outlet-corroborated figure (Hormel ~$11M, Tyson's cattle-beef entry needs its two figures split out) before any comparison UI reads these fields. (B) Recalls got four different treatments across the 8 files (`enforcement`+`'settled'`, `enforcement`+`'adjudicated'`, `practices`+`'government-record'`, `practices`+`'company-disclosure'` — the last one was a real mislabel, fixed same day) — the schema has no dedicated home for a recall, so a third-party co-packer's minor recall and a 10-death listeria outbreak with an FSIS sanitation finding can end up wearing the identical badge. Fix: add a `recalls[]` array or a `'recall'` status to evidence-schema.md before the UI build. (C) `gapStep` — the module's headline standardized cross-species field — splits on which GAP directory page a researcher happened to check: 3 companies (Boar's Head, Dietz & Watson, Land O'Frost) got `'none'` from GAP's Shoppers/retail-partner page, while 5 (Tyson, Hormel, Kraft Heinz, Smithfield, Johnsonville) got `'unknown'` from the Manufacturers directory or a failed query — so three small brands currently read "confirmed no GAP" and the two biggest companies read "no data," purely as an artifact of which page loaded, not a real difference in GAP status. Fix: re-run all 8 against one consistent GAP directory before this field is used for any cross-company comparison | Opus review (Stage 5) surfaced these as structural, batch-level issues distinct from the 12 blocking + 4 minor per-file defects it fixed directly — correctly declined to guess a fix for any of the three since each needs either new research (C) or a schema decision (B) or a policy call on which figures count as "corroborated enough" to populate (A) | Not yet actioned — tracked here so it's ready when meat-poultry's UI build starts, same sequencing the egg pilot followed (data merge first, UI decisions second, never guessed ahead of real research) |
| 2026-07-30 | **meat-poultry and seafood modules activated** (sourcing-transparency pipeline, next industries after eggs). Founder scoped "cows, pork, lamb, fish" → meat-poultry (Boar's Head, Dietz & Watson, Hormel, Johnsonville, Kraft Heinz, Land O'Frost, Smithfield, Tyson — 8 companies, ~75 products, 3 hummus products excluded as plant-based/out-of-module) + seafood (Bumble Bee, Rich Products/SeaPak, StarKist, Wild Planet — 4 companies). Founder explicitly reordered claim-type priority BEFORE any agent spend, correcting the initial ask to also route this through Octavius (wrong agent — Octavius does product/label/ownership research, not welfare/evidence-schema research; corrected, kept on the dedicated sourcing pipeline's own Sonnet/Opus agents): living conditions/housing (GAP step level, cross-species) and intentionally-misleading claims are PRIMARY — specifically grass-fed-vs-grain-finished decode (beef/lamb, the single biggest misleading claim in the category), gestation-crate status (pork, the pork equivalent of egg cage-free), stress/handling standards, air/water-chilled (poultry, labeled as processing not welfare); certifications generally, "no antibiotics ever," USDA-FSIS enforcement, and labor practices stay in scope but SECONDARY (feed the score, not the card's headline). Seafood's primary claim reframed the same way: decode what "farmed" actually means (open-net vs. closed-containment/RAS vs. wild-caught — materially different practices lumped under one word) over a flat cert list. `industry-modules.md` rewritten accordingly before Stage 2 started, so researchers are briefed correctly | Founder: "what about their living conditions, thats what im most interested in... the certs can all be included and part of the scoring but its not primary focus" — same framing that drove the eggs card (housing ladder as the headline, certs as supporting evidence), now made the explicit standing rule for every future module rather than eggs-specific | Stage 0 catalog audit done free (zero companies had ANY sourcing data outside eggs — genuine from-scratch activation, not backfill). Stage 1 primary-source pull run (`verify-primary-sources.js`, real hits: Tyson/Hormel/Kraft Heinz 10-Ks, In re Broiler Chicken Antitrust Litigation docket naming Kraft Heinz Foods Company + Tyson Breeders Inc. as parties, In re Pork Antitrust Litigation and In re Cattle and Beef Antitrust Litigation MDL dockets, StarKist/Bumble Bee tuna litigation leads) — checkpointed to `src/data/research/sourcing/meat-poultry/_primary.json` and `.../seafood/_primary.json`. Stage 2 wave 1 launched (3 concurrent Sonnet research agents, respecting the max-3 cap from the /political-analysis incident): Tyson/Hormel/Kraft Heinz, Boar's Head/Dietz & Watson/Land O'Frost, Johnsonville/Smithfield. Seafood (StarKist+Bumble Bee, Wild Planet+Rich Products) queued as wave 2, launches once a wave-1 slot frees up. Nothing merged yet — this entry covers the module activation + research kickoff only |
| 2026-07-30 | **Living Conditions card: 'organic' tier housing photo changed from free-range.jpg to cage-free.jpg.** Founder: "they should be seeing the cage image if they are not caged free or pasture." Flagged the accuracy wrinkle before implementing (asked, didn't guess): USDA organic rules legally prohibit battery cages outright, so caged.jpg would misstate the housing SYSTEM even when the real number is bad; free-range.jpg (the original default) turned out too generous given the just-verified real data (Eggland's Best's organic line: 1.2 sq ft/hen indoors, below the cage-free minimum). Founder chose cage-free.jpg — legally correct (no cages) and honest about what the real audited numbers actually look like — over literal caged.jpg. Separately confirmed free-range keeps its OWN distinct photo (founder's wording named only "cage-free or pasture" as exceptions, but on being asked directly, free-range's real regulatory distinction — no cages plus continuous outdoor access — earned it staying separate rather than folding into organic's treatment | Same organic-line audit that produced the comparison-baseline feature (below) — the founder's own instinct ("basically a trick") was directionally right but the literal ask (cage photo) would have been a distinct new inaccuracy (organic can't legally use cages) traded for the one it fixed | Live, 410/410 tests still passing (pure asset-mapping change, no test pinned the old photo) |
| 2026-07-30 | **Living Conditions card: per-brand space figures now always pair with a comparison baseline** (`getComparisonBaseline()` in `sourcingMatch.js`). Follow-up to the same-day re-architecture below — founder hand-checked the two real per-brand figures against each other (Vital Farms 108.9 sq ft/hen vs. Eggland's Best 1.2 sq ft indoors) and correctly flagged that a bare number has no context: is 1.2 sq ft good, bad, or average? The card now shows the tier's generic standard alongside any per-brand figure so the reader can judge it. 'organic' has no generic standard of its own (never-fabricate gap, see below) but USDA organic rules prohibit caging outright, so its baseline is the cage-free floor (1.5 sq ft/hen), explicitly noted as a regulatory floor, not organic's own standard. Rendered unconditionally alongside every per-brand figure — never shown only when a brand looks bad, to hold the symmetric-fairness rule | Founder: "the Eggland's Best (Organic line) is basically just a trick, the chickens are traditional with a slightly healthier diet" — checked this against real numbers rather than taking the framing at face value: literally true by the numbers (1.2 sq ft is below even the cage-free floor, despite the organic label), but "trick" itself doesn't get written into the app — the two numbers next to each other let the user draw that conclusion themselves | Verified against the two real catalog cases: Vital Farms' organic-pasture line (108.9 sq ft/hen) essentially matches its own pasture-raised standard (108 sq ft/hen) — expected, since Vital Farms actually holds that certification. Eggland's Best's organic line (1.2 sq ft indoors) comes in under the cage-free floor (1.5 sq ft/hen) it's legally required to clear — the real, surprising finding a bare number would have hidden. 410/410 tests passing (4 new) |
| 2026-07-30 | **Living Conditions card re-architected from company-gated to product/name-gated** (`sourcingMatch.js`, `LivingConditionsCard.js`, `ProductScoreScreen.js`, `productParser.js`). Founder pushback: the card was described as needing company-level `sourcing` research, but the tier (badge/photo) was already read from the product's own name — only the space-per-hen figure was hard-gated on a company having a Cornucopia scorecard entry (2 of 15 companies). Added `GENERIC_TIER_STANDARDS`, a tier-keyed table of each housing term's OWN published certifying-body standard (Certified Humane: cage-free 1.5 sq ft/hen indoor, free-range 2 sq ft/hen outdoor + 6hr access, pasture-raised 108 sq ft/hen; United Egg Producers Certified: conventional 67 sq in/hen) — real, sourced, cited to the standard body's own page, verified live via WebSearch (organic deliberately left with no figure; the one fetchable primary source 403'd, never-fabricate rule). New shared `isEggsHousingEligible()` replaces the `company?.sourcing` hard gate everywhere (also excludes plant-based egg substitutes like JUST Egg by name, a module-boundary rule that used to ride on the company gate accidentally). A real per-brand scorecard figure still wins when we have one; the generic standard is the honest fallback, never both stacked. Also fixed two bugs found while re-tracing the full card path: `buildProductFromRaw()` trusted OFF's `categories_tags[0]` blindly for category, which is ordered general→specific and so almost never says "eggs" for an out-of-catalog scan — the actual reason "extend to any scan" wouldn't have worked even after the company-gate fix; and `stickyHeaderIndices` math never accounted for the community-source banner as a conditional sibling (flagged 2026-07-29, never fixed — now folded into the same stickyIndex formula the Living Conditions card already feeds) | Founder: "this isn't company based code, it should be based on product and the name of eggs" — correct: the pilot's own module-boundary finding ("most brands sell multiple tiers under one name") already established product.name as the ground truth for tier; the space figure just hadn't followed that same logic | Verified against all 33 real catalog egg products directly (not a sample): 32/33 (all non-plant-based) now show a sourced space figure, up from 2/33 before — 3 keep the more specific per-brand Cornucopia figure, 28 gained the generic certified-standard figure, 1 (plain "organic," no further tier claim) correctly shows nothing rather than a guess, JUST Egg correctly excluded. 406/406 tests passing (13 new: genericStandard behavior, `isEggsHousingEligible`, category-tag detection incl. an eggplant false-positive guard). No company research pass was needed — this was a code-architecture fix, not a data-coverage gap. Not yet done: no real device/preview verification possible for this session (RN screens, no browser-testable surface) — flagged per standing policy, logic-level verification only |
| 2026-07-29 | **Housing-imagery disclosure policy FINALIZED** (after a same-day correction): no per-image in-app caption after all — founder rejected it directly ("the image alone is enough disclosure"), which was my own over-add on top of what he'd actually asked for. Settled two-layer policy: (1) real IPTC `DigitalSourceType`/`trainedAlgorithmicMedia` XMP metadata embedded in every generated PNG via a new pure-Node script (`.claude/skills/sourcing-transparency/scripts/embed-ai-metadata.js` — no exiftool/npm deps available on this machine, so it hand-builds the PNG `iTXt` chunk + XMP packet; verified by parsing the embedded chunk back out, round-trips correctly), run on both egg-pilot generations; (2) one blanket disclosure line added to `web/terms.html` Section 4 ("Accuracy of Information") — general, not per-image, covers every current/future use of this pattern | Closes the reputational risk I'd flagged (undisclosed AI imagery in a "we show you what's real" app) without per-image UI clutter or maintenance burden | Both images now metadata-tagged; terms.html updated and ready to deploy via the existing Netlify pipeline. Policy also written into `references/housing-imagery.md` and durable memory (`feedback-ai-image-disclosure-policy.md`) so this doesn't get re-litigated to a different answer next time an industry module needs housing imagery |
| 2026-07-29 | **Housing-imagery workflow set** (for the future `sourcing` UI, not yet built): real licensed photos first (USDA ARS/Wikimedia — but verify the license on the file's OWN page, never infer from a filename pattern; a Wikimedia "USDA-looking" caged photo turned out to be Ontario provincial government-licensed, not USDA); avoid advocacy/investigation photography as a source (rights + tone problems, Wikimedia's own Battery-cages category is dominated by it); Pinterest ruled out (JS-rendered pages return nothing to WebFetch; pins strip original license anyway). Where no free real photo exists (cage-free/aviary interior), AI generation is the fallback — text-to-image ONLY, never image-to-image off a copyrighted photo (single or multiple — "dilute traceability across several references" was proposed and explicitly rejected: doesn't reduce rights exposure, multiplies rights holders used, Getty Images is actively litigating this exact pattern against AI companies); depicts the generic system only, never a specific company's facility; tone must be flat/documentary, not glossy — a golden-hour-lit "beautiful" caged-hen photo visually undercuts a paired real figure like "1.2 sq ft/hen." Disclosure: no visible burned-in banner (undercuts the image's purpose); instead invisible IPTC metadata tagging (detectable by analysis, not casual viewing) + a normal small in-app credit-line caption wherever displayed — same treatment every other sourced claim in this feature gets | Founder wants realistic housing-tier photos to drive users toward healthier alternatives, but flatly refused to pay for stock ("that's dumb") and pushed back hard on an early "hypocrisy" framing — correctly: AI-generated is not itself deceptive if disclosed, the real question is whether disclosure survives to where people see it | Two images generated (cage-free aviary; caged hen at feed trough, second pass after founder critique of the first pass's too-glossy lighting), founder approved both. Process written to `.claude/skills/sourcing-transparency/references/housing-imagery.md` + linked from SKILL.md so it's reusable for dairy/meat-poultry/seafood/produce-farms tiers later. **Not yet done: the metadata tag and in-app caption** — generated files currently carry neither; do this before any ship into a build |
| 2026-07-29 | **Living Conditions UI shipped** — product-scoped (not company-scoped) housing card on ProductScoreScreen, directly below "Food Exposé Says": tier read from the product's own name (most brands sell multiple tiers under one name), photo + founder's exact caption ("This animal lived in conditions similar to this."), certified space-per-animal figure when one matches. A company-level "Sourcing" tab (built first, before the founder redirected to product-scoping) survives as the linked-out "full record" destination rather than being discarded. Bundled 4 housing-tier photos (2 real CC-licensed, 2 AI-generated with invisible XMP provenance tags per the settled disclosure policy) and backfilled 2 fact-check-verified space-per-animal figures (Eggland's Best 1.2 sq ft, Vital Farms 108.9 sq ft/hen) as a new `scorecards[].spacePerAnimal` schema field | Founder was explicit the whole point of this feature is showing the DIFFERENCE between a brand's own tiers — a company-wide aggregate view (what got built first) can't do that since it has no idea which SKU a user is holding | Committed `7480257`, `4db83af`. **Real bug caught by a full-catalog review sweep, not just unit tests**: the certification scope-filter only checked for exclusions on the conventional tier, so every other tier let a cert through unfiltered — "Eggland's Best Cage Free" was surfacing a Certified Humane badge whose own scope text names only free-range/pasture-raised. Fixed (an "only"-qualified scope is now read as an exhaustive list). Also fixed: 'organic' tier defaulted to the caged photo, which USDA organic rules legally prohibit — remapped to free-range as the closer generic match. Flagged separately (not fixed, pre-existing, out of scope): `stickyHeaderIndices` math on this screen doesn't account for the community-source banner as a conditional sibling, likely misplacing the sticky tab bar for unverified-source products |
| 2026-07-29 | **Egg sourcing pilot MERGED** — `sourcing` field (animal-welfare certifications, Cornucopia scorecard ratings, litigation/enforcement, sourcing model) now live on 15 COMPANY_DB records, additive, 373/373 tests green. Full pipeline run: 15-company research → fact-check → a local SEC/CourtListener script (bypassing the 403s that blocked every agent) → Sonnet writers (founder directive: writer=Sonnet, reviewer=Opus, not Opus/Opus) → one independent Opus legal/praise-rail review → main-session merge via a new brace-matched `merge-sourcing.js` script. Data-only; no UI built yet | Validate the `sourcing-transparency` skill on real data before scaling to other industries, per the founder's ingredients+companies repositioning idea (backlog, 2026-07-28) | Review found 5 blocking + 7 minor defects, ALL running in a company's favor (an unverified self-claimed certification published as fact; a settlement attached to a brand whose ownership was itself unconfirmed; a competitor's scorecard rating on the wrong company; two retailers' own lawsuits mislabeled as actions against them). All fixed pre-merge. Structural lesson banked in the skill: every defect was a *placement* error (correctly-hedged prose in a structured field whose schema asserts more than the prose does), not a language error — Stage-4 checklist updated accordingly. Two real data-quality bugs found and fixed as a side effect: Horizon Organic was misattributed to `danone` (Platinum Equity acquired majority control 2024-04-02) — fixed in a2c62ef; a Happy Egg lawsuit citation had the wrong year (2022 → actual 2021-10-15) — fixed in 635fdcc. One false lead traced back to the session's own first-pass parsing error and closed: no Gold Circle Farms / Nature's Yoke catalog mapping error exists — confirmed directly against products.js twice. **Not yet built: any UI surfacing of `sourcing` data** — that's a separate feature decision, and the founder's photo-per-housing-tier idea (real animal photos illustrating housing systems generically, never claimed to depict a specific company's facility) is designed but unbuilt |
| 2026-07-28 | **Scanner is now local-first**: curated catalog is checked BEFORE OpenFoodFacts, so our reviewed data wins over live third-party data for every barcode we carry (previously OFF won, local was only a fallback). Barcode fetch bounded by the shared `fetchWithTimeout` (RN fetch has no default timeout); Connection Error alert replaced by an in-screen Specs card whose Retry re-runs the same barcode and which offers "Request this product instead" | Founder hit recurring "Connection Error" scanning in-store on LTE, sometimes after a long hang on "Found!". Probing OFF confirmed intermittent 503s (HTML bodies) from `world.openfoodfacts.org` — the same host the scanner uses. Local-first also closes the OFF-vandalism exposure for carried products | Committed `0d7cd6c`. Rides the next founder-run EAS build |
| 2026-07-28 | **Ingredient-filter heuristics must be calibrated against the whole catalog before adoption** — never ship one on the strength of the failing example alone | Fixing OCR'd package text rendering as ingredients (Gorilla Mind scan: "Boise", "Cans Per Day", warning sentences scored as food). The first three drafts of the weak-hit screen each looked right on the bug but would have hidden 8,160 / 3,708 / 2,771 REAL curated ingredients ("bay leaf", "vitamin b2", "imported olive oil"); merge-splitting without an all-compound-names rule shredded ~15k ("blackberry juice concentrate" → blackberry + juice concentrate) | Committed `21c47f1`. Final heuristics hide 29 tokens (all genuine boilerplate) and move 22/2000 sampled product scores, each traceable to an ingredient a merged token was hiding. 373 tests green |
| 2026-07-28 | Feature requests built as a **mirror of the product-request loop** (local-first save → best-effort sync to a write-only Supabase table → merged into My Requests with a Feature chip), not as an upvotable public board | Founder asked for it "like we did product request" after seeing a WishKit/feedback-board thread. The mirror reuses a proven offline-safe path and needs no client read access; a public board would need SELECT policies, vote tables, and moderation — not justified at current user count | Committed `1177152`. **Founder action required: run `supabase/feature_requests_setup.sql` once in the Supabase dashboard**, same as product_requests |
| 2026-07-26 | **All 19 null-companyId products resolved (+16 new company records), and the company screen stopped asserting money-trail facts nobody researched.** Ownership research (Octavius, sources per claim) caught three traps that brand-name recall would have gotten wrong, two of which had a *wrong-but-existing* id sitting right there: **Muir Glen is no longer General Mills** (sold to Violet Foods / Amphora Equity Partners, announced 2026-01-27) and **Tazo is no longer Unilever** (the whole ekaterra tea business went to CVC, completed 2022-07-01, now Lipton Teas and Infusions); third, Crunchmaster's parent TH Foods became a wholly owned Kameda Seika subsidiary in June 2025. Both traps re-verified independently against primary sources before applying. Applying them exposed a **much larger pre-existing problem**: the Pro company page was presenting absent data as findings — 144 of 268 companies carry `lobbyingSpend: 0` (an "unresearched" sentinel, not a verified zero) and each rendered a green "Lobbying Risk: Low — $0/yr"; **131 companies rendered a fabricated 50/50 political donation split** that exists nowhere in the data (a `?? 50` fallback plus a `!= null` guard that let the 0 sentinel through); 26 displayed the literal string "$null" as revenue. Fixed in `f861b3c` — 0 and null both read as "No data", money figures gate on `> 0`, donation split requires a real split object, missing revenue/employees hide or read "Not disclosed". New records use `null` (never 0) for unknown figures and carry no political data at all — that stays with `/political-analysis`. Two new inert fields, `parentCompany` + `ownershipSource`, keep the verified chains and provenance in-repo | Founder picked the null-companyId gap as one of three focus items ("company transparency is your Pro gate; every gap is a user hitting the paid feature and finding nothing"), then said to apply the research | `f861b3c` + `aaf43e1`. 307 → 323 tests green. 268 → 284 companies, 19 → 0 null companyIds, zero dangling refs, products.db rebuilt + validated PASS. **147 of 268 existing company pages change appearance — deliberate, they were showing conclusions drawn from missing data.** Research's own summary miscounted ("12 distinct new companies" vs. the 16 its table required) — caught in review. **products.db NOT uploaded and DB_VERSION NOT bumped on purpose**: a fresh install of the currently-live build would pull a catalog referencing companies its bundled `companies.js` lacks and show blank company pages, so upload only AFTER the binary carrying this ships. Open: whether the 144 `lobbyingSpend: 0` records should become `null` in the data (needs founder go-ahead, visibly changes 144 pages), and `celsius-holdings` has `politicalDonations: 0` with a hardcoded 50/50 split baked into the record itself |
| 2026-07-26 | **`npm test` was inflated by stale git worktrees — real suite is 307 tests, not 987.** Three abandoned worktrees under `.claude/worktrees/` (leftovers from agent runs) each hold a frozen copy of `src/`, and jest discovered and ran all of them: 680 of the 987 "passing" tests were stale snapshots testing their own old code, which can't catch a regression in what ships. `.claude/` is gitignored but jest doesn't read `.gitignore`, so it needed an explicit `testPathIgnorePatterns` (which also has to restate `/node_modules/`, since setting it replaces the default rather than extending). The worktrees themselves were left in place — they may hold unpushed agent work, so removing them is the founder's call, not a config fix's side effect | Noticed while reviewing a build report that cited "976 → 987 tests" as evidence of coverage; the PASS lines showed worktree paths | `b7bea94`. Any test count in this log dated before today may be similarly inflated — treat 307 as the real baseline going forward |
| 2026-07-26 | **Sentry source-map/symbolication wiring landed — the long-open TODO in `sentry.js` is closed in code, but needs 3 EAS secrets from the founder before it does anything.** Added the `@sentry/react-native/expo` config plugin to `app.json` (adds the upload build phase to the generated native projects), an `npm run upload:sourcemaps` script for the OTA leg, and `.env.sentry-build-plugin` to `.gitignore`. Org/project resolve from `SENTRY_ORG`/`SENTRY_PROJECT` env vars rather than being hardcoded, because the slugs aren't recorded anywhere in the repo — verified the generated `sentry.properties` falls back to env for all three values and writes **no token to disk**. **The find that mattered:** `sentry-xcode.sh` exits 1 when an upload fails, which fails the entire build — a missing or fat-fingered token would have burned a metered EAS build on a diagnostics-only step. Guarded with `SENTRY_ALLOW_FAILURE: "true"` in `eas.json` (preview + production), which per Sentry's own error message downgrades a failed upload to a warning. Trade-off accepted and written into the SOP: a broken upload is now SILENT, so the build log must be checked for `sentry-cli - error` the first time after touching the secrets. Also documented the OTA gap: `eas update` ships JS the build-time upload never saw, so an un-uploaded OTA silently reverts that release to minified traces — hence the new SOP section | Founder picked option 2 after the release-tagging work; the credential itself is his to create and install (Claude never handles auth tokens) | Code complete, 976/976 tests pass, `expo config --type prebuild` resolves the plugin clean. **Inert until the founder runs three `eas secret:create` commands** (SENTRY_ORG, SENTRY_PROJECT, SENTRY_AUTH_TOKEN with `project:releases` scope) — and it only takes effect on a real build, since a config plugin changes the native project. Offered but not done: hardcoding org/project in `app.json` (they're not secrets) to drop the setup from 3 secrets to 1 — needs the founder's org slug; the project slug looks like `react-native` from his dashboard screenshot but was NOT assumed |
| 2026-07-26 | **Sentry now tags every event with `release`/`dist` + OTA update id.** Founder surfaced a Sentry list still showing `ReferenceError: Property 'setUserName' doesn't exist` daily through 07-26 — a crash already fixed in `ff411a0` (2026-07-22, dead setter left behind when Home's greeting moved to `useAuth`; it threw on *every* Home-tab focus). Diagnosis: source is clean, the events are old binaries still installed, and pre-1.2.0 builds predate `expo-updates` (`c474a60`) so they can't be OTA-patched — they need the App Store update. Root problem exposed: nothing in `src/utils/sentry.js` identified which build an event came from, so "is this fixed in the shipped build?" was unanswerable from the dashboard. Fixed by setting `release` as `<bundleId>@<version>+<build>` (Sentry's mobile convention, matching what the native SDK auto-detects so grouping and future source-map uploads line up), `dist` as the build number, and four `ota.*` tags. **The OTA tags are the real win**: once `eas update` is live, a patched and an unpatched device report the *same* native release — `ota.update_id` is the only field that separates them, so it's what you filter on to confirm a JS fix actually landed on devices. Values come from `expo-application` + `expo-updates`, both already linked natively (transitive deps of expo-notifications/auth-session), so this adds **no new native module** — it can ride an OTA update, not just a build. `expo-application` promoted to an explicit dependency at the version already resolved (one-line lock change, zero version churn) | Founder picked option 1 of 3 after the crash diagnosis; symbolication (option 2) still open, needs his Sentry org/project slugs + auth token | 976/976 tests pass (8 new in `src/utils/__tests__/sentry.test.js`, pinning the release format, the null-natives fallback, and the OTA tag shapes); `expo export` bundle smoke clean. Note the release string is only as good as `app.json`'s build number, and the existing config-plugin/source-map TODO at the top of `sentry.js` is untouched — stacks stay unsymbolicated until that lands |
| 2026-07-26 | **Microplastics video v3 — final cut, 48.75s.** Founder review of the 50.9s version flagged two defects, both root-caused rather than guessed at: (1) *"his voice is slightly garbled on the heart scene"* — traced to the **source TTS**, not the mix or the speed-up (mix peaked −3.9 dB, no clipping; whisper transcribed the raw Higgsfield audio as `"four -and -a -half times"`, the hyphens indicating clipped prosody). Fixed by testing three phrasings against transcription until one read clean: "four and a half" garbles, "4.5 times" fragments, **"nearly five times" is clean**. (2) *"the first twelve seconds feel faster paced than the rest"* — traced to blocks 1 and 2 being 6s/7s long while their Seedance animation is only 5.04s, so both **ended on a frozen frame**; motion sprinted then stopped dead while later blocks animate full-length. Fixed by stretching each clip's motion to fill its block (0.84x / 0.72x) instead of freeze-padding, same durations and audio sync. Also completed the silence cleanup (5.5s → ~1.5s): vein clip 1 sped 1.68x rather than trimmed (trimming would amputate the dense end state), heart block trimmed to fit the new line, and audio now leads each cut by 0.15s. | Founder asked for the silence cleanup, then caught both defects by ear/eye on review | **APPROVED AS FINAL, cleared to post** ("We are using it as is. Its good"). One accuracy item was raised twice by Claude and consciously accepted by the founder rather than fixed: "nearly five times" rounds UP from the study's real hazard ratio of 4.53 — rationale is that 4.53 genuinely is nearly five and the exact figure sits in the NEJM paper cited alongside the post. **Logged as a decision, not an oversight — do not reopen.** Also delivered: verified public source list (PNAS/NEJM) including the published methodological critique of the bottled-water paper, so the founder isn't blindsided in comments |
| 2026-07-26 | **Microplastics video BUILD COMPLETE** — 8 blocks, 58.46s master, founder-selected 1.15x cut at 50.9s ("This was really good"). Structure changed substantially from the original 9-scene plan: scene 2 redesigned (water disperses stomach→whole-body veins), old scene 3 replaced by a 2-clip vein/circulatory sequence, scene 4 artery rebuilt (clean start → total occlusion, replacing a version that had microplastics pre-present and only partial narrowing), a heart-attack clip added alongside the beating heart, the FDA caveat scene CUT as "extra words," and the CTA rebuilt with the Specs backflip composited over the final scan frame. Arthur (Higgsfield preset) narrates throughout, one voice. **A proposed VO line was caught and killed as factually false before use** — "your body has no way to filter plastic out" is wrong (larger microplastics ARE excreted; that's how exposure is measured), replaced with the accurate "The bigger pieces pass through. The small ones stay." **Three assembly bugs found and fixed:** `explainer_video` centers short audio inside each block, padding silence before AND after every line (80s video for 53s of speech, 2-6s dead-air gaps) — replaced with a manual concat + `adelay`/`amix` timeline, cutting silence from ~22s to ~5.5s; its built-in captions are phrase-level and lag, so word-level captions were rebuilt from a `faster-whisper` transcript of the real mixed audio (with fragment-merging so `240` + `,000` renders as one caption, and per-word font auto-shrink so long words don't overflow); and `-c copy` concat of clips from different encoders produced decode errors + a wrong duration, fixed by normalizing every block before splicing. | Founder ran a full review pass on the first assembly and rejected it: captions off-base and phrase-level, video/audio duration mismatch, scene 2 "looks weird," "4.5x" read aloud as "four point five ex," redundant bloodstream scenes | Done, not posted. Full detail + all media IDs + a hard-won lessons section in `marketing/videos/microplastics-video-progress.md`. Optional remaining polish (silence cleanup to ~47s) offered and deliberately not executed. Also logged: Seedance false-positives NSFW on this gray anatomy body (Kling doesn't), and the Higgsfield sandbox does not persist between calls |
| 2026-07-24 | **Microplastics video: scene 1 continuity fixed (gray anatomical body drinking, matching scenes 2-6, replacing the human-drinking opening) + a reusable 6-image anatomy reference sheet built and locked for future videos beyond this one.** Built via master-shot chaining (same technique that fixed the Christian sheet): generated one neutral gray-body pose, founder-confirmed it, then generated each of 5 system-highlight variants (nervous/yellow, circulatory/red-blue, organs/natural color, muscles/red, skeleton/ivory) off that confirmed pose — held identity and pose consistent across all 6 on the first pass, no drift/redo needed this time. Composite saved to `marketing/assets/anatomy/microplastics-anatomy-reference-sheet.png`. All 6 also saved as **Higgsfield Elements** (workspace-level reusable references, `show_reference_elements`) so future prompts can pull them in via `<<<element_id>>>` instead of tracking job IDs by hand — first use of this Higgsfield feature in the project. | Founder: circle back to the paused microplastics video, noticed scene 1 broke visual continuity, and separately wanted a general-purpose anatomy body ("we could totally see us using that very often as an element for videos") | Founder-approved. Scene 1 has a new still frame but still needs the Kling animation pass before it's a finished clip — tracked in `microplastics-video-progress.md`. Anatomy sheet is intentionally scoped as reusable beyond this video (e.g. muscles for a fitness post, organs for a digestive-health post) |
| 2026-07-23 | **Satire carve-out added to the never-fabricate rail: fabricating the app's OWN interface/output (e.g. a joke "screenshot" giving candy a perfect score) is allowed for marketing.** Added to CLAUDE.md, `instructions/instructions.md`, and `content-framework.md`'s "off-limits" section as a scoped exception, not a repeal — fabricating THIRD-PARTY facts/data (fake studies, fake company stats, fake regulatory quotes) is untouched and still fully banned; this only covers depicting the app itself doing something it doesn't really do, for obvious comedic effect. Claude raised two concerns before implementing (reshare-without-context risk given the app's core promise IS scoring trustworthiness; the shape-match to the 2026-07-13 OFF vandalism incident) — founder heard both and overrode: reshare-without-context is a generic risk not specific to this format, and the vandalism incident was an accidental live-app bug he found himself, categorically different from an intentional labeled marketing bit. One nuance kept as a review-time judgment call rather than a hard rule: the more photoreal/convincing a fake is, the more the caption/framing/absurdity of the claim has to carry the "this is a joke" signal (relevant here since the founder separately wants "indistinguishable from real" production quality) — this is a satire-vs-deceptive-advertising line, not a brand-voice one, so it's flagged for the Opus/founder review pass to actually check, not just assumed. | Founder: "I don't mind if someone reshares it without the joke... it's just marketing, you do crazy stuff sometimes to draw customers, create buzz" | Doctrine updated same session. No content drafted yet under this carve-out — next real test is whichever satire piece Cicero drafts first |
| 2026-07-23 | **5th recurring cast member added: "Christian," a real-likeness founder persona (6'0", ~350 lbs, getting-in-shape story arc), locked at `marketing/assets/cast/christian-founder-character-sheet.png`.** Generated from an uploaded real photo of the founder via Higgsfield (`nano_banana_pro`/`nano_banana_2`). Two real problems surfaced and fixed during generation, not just prompt wording: (1) single-shot multi-panel requests (all 9 views/expressions/poses in one generation) caused visible face/body drift on ~3 of 9 panels and repeated near-duplicate front-facing angles — root cause is asking one generation to hold identity+mass consistent across 9 invented angles from just one frontal source photo; (2) "concept-art / polished line work" language in the prompt pushed the model toward an illustrated look when the founder wanted true photorealism. Fixed via **master-shot chaining**: generated and founder-confirmed a single correct front-view photo first, then generated each of the other 8 panels (side, back, 3 expressions, 3 poses) as its own image referencing that confirmed shot instead of the raw source photo — held both face and body-size consistency across all 9. Final sheet assembled from the 9 approved images with a local Python/Pillow script (deterministic composite + labels + material callouts), not a 10th AI generation — avoids re-introducing drift into a sheet that was already correct panel-by-panel. `ai-video-style-guide.md`'s Recurring Characters list updated with a #5 entry documenting the technique for future reuse. | Founder uploaded a real reference photo and iterated through 4 rounds of feedback (bigger, more photoreal, fix drift/fat-loss) before landing on the master-chaining approach | Founder-approved. ~9 generations run (~18-20 credits) across the session. `marketing/` is gitignored so the asset isn't in git, but this decision-log entry and the style-guide edit are tracked |
| 2026-07-23 | **Two doctrine changes: (1) never-fabricate rail scoped to facts, not tone — marketing exaggeration explicitly allowed. (2) Founder confirms his real likeness in marketing is welcome, including a stylized/exaggerated "before" persona for a getting-in-shape story arc.** (1) added as a caveat to CLAUDE.md's non-negotiables, `instructions/instructions.md`'s Never list, and `marketing/strategy/content-framework.md`'s "rail is ACCURACY" section: hyperbole, dramatic framing, punchy hooks, self-deprecating bits are fine; the hard rail stays no invented number/quote/study/screenshot presented as real, no medical-causation claims. (2) extends the 2026-07-05 "no founder face" reopening (real founder-filmed video) — now explicitly covers a stylized/AI likeness too, not just real footage. `ai-video-style-guide.md`'s Founder presence section updated; the existing military-identifier rail (JER/DoDD 1344.10) still applies to any frame showing him, real or stylized. | Founder: "add a caveat that we're able to exaggerate for marketing/views" + "I have no problem with the founder myself being used in marketing... I actually like it being Fat me" | Doctrine updated same session. A "Fat me" getting-in-shape character sheet was mid-discussion when this came up (paused pending this policy clarification) — now unblocked to proceed as a literal-likeness stylized character, not a fictional stand-in |
| 2026-07-23 | **1.2.0 build submitted to Apple.** Carries the rename to "Food Exposé: Barcode Scanner" + new Specs icon, the onboarding/paywall overhaul, the 2026-07-22 submission-preflight fixes (first-name capture disclosure, dead support-email fixes, legal-link repoint to `shelfexpose.app`), the `.easignore` tarball fix that unblocked the upload, and `expo-updates` (OTA capability, first build to carry it). App Store Connect metadata finalized same window: Support/Privacy/Marketing URLs confirmed live (`shelfexpose.app/support`, `/privacy`, and the root, all verified resolving through the Cloudflare redirect to the real Higgsfield-hosted pages), keywords revised to drop title-redundant terms (food/scanner/barcode already covered by the App Name) and swap in `organic`/`GMO` for `companies`/`clean` | Founder queue of fixes from the preflight pass + ASC metadata pass was ready; no further blockers found | Submitted, awaiting Apple review. Once approved: this is the first build with `expo-updates` live, so future JS-only fixes can go out via `eas update` instead of a new build/review cycle |
| 2026-07-22 | **`marketing/` reorganized into category folders; supersedes the 2026-07-08 decision to pin shared exemplars/strategy docs at `marketing/` root.** New layout: `strategy/` (MARKETING-PLAN.md, content-framework.md, making_shelf_expose.md, content-audit-month-01.md, 30-day-launch-plan.md, x-posts-growth-batch.md — the old root-exemplar set), `mascot/` (mascot-strategy.md, specs-animation-prompts.md), `scripts/` (video-scripts-batch.md, video-scripts-batch-2.md, runway-scripts-label-translator-producer-motive.md), `videos/` (ai-video-style-guide.md, higgsfield-video-workflow.md, microplastics-video-progress.md, convenience-cost/ — moved out of the old `video-projects/`), `brand-assets/` (shelf-expose-website-top-background.png, renamed off spaces). `2026-07/`, `2026-08/`, `journal/`, `app-store-screenshots/`, `_archive/` unchanged. Every cross-reference to a moved file was swept and fixed (cicero.md, content-sprint SKILL.md, CLAUDE.md, backlog/chad-tasks/company-map/notification-copy-bank/sops/priorities.md, plus internal links inside the marketing files themselves) — verified zero broken links repo-wide after. Note: the founder was manually moving a few root files into category folders in a separate window at the same time as this pass (mid-edit, not a conflict) — final placement reconciled with his moves | Founder: "we just keep making new files... that drives me crazy," wanted videos/mascot/scripts category folders, month-first dating preserved, confirmed (when asked) that nothing should sit loose at root anymore — not even the old exemplar set | Done same-session. `marketing/` is gitignored so the moves aren't in git; the doctrine-file fixes above ARE tracked and were committed. Founder can spot-check the new layout; nothing was deleted, only moved |
| 2026-07-22 | **EAS build failed uploading a 147MB project tarball (GCS signed-URL ECONNRESET); root-caused to `.easignore` gaps, not app size.** `.easignore` REPLACES `.gitignore` for EAS uploads rather than extending it, so `marketing/` (gitignored, 81MB of ad creative/screenshots/video projects) was never actually excluded from the build archive. Also found and excluded: `src/data/batches/` (14MB of Octavius product-pipeline staging data — raw fetches, QA reports, intermediate batches — confirmed zero runtime imports) and unused mascot asset-pipeline masters (`assets/mascot/*.mp4`, `/transparent/`, `specs-reference.png` — 14MB, confirmed only `webp/*.webp` + `specs-standing.png` + `grocery-aisle-bg.jpg` are actually `require()`'d anywhere). Also found and flagged (not yet removed): `playwright`/`playwright-extra`/`puppeteer-extra-plugin-stealth` sitting in production `dependencies` despite being unused by any app or script code — doesn't affect tarball size (node_modules is excluded) but is dead weight worth removing | Founder reported a failed build with a cryptic GCS XML error; traced "147 MB" in the upload progress line to the tarball, not the compiled IPA as first assumed | 108MB removed from the upload archive (~147MB → ~39MB estimated). 968/968 tests pass (upload-config-only change, no app code touched). Not yet committed — founder to retry the build |
| 2026-07-22 | **Submission-preflight pass run before the 1.2.0 build; 4 real findings fixed.** (1) Live downloadable `products.db` catalog was stale — 1,045 rows vs. 1,092 in current `products.js` (missing the 4th cowork batch + Planters/sahale-snacks fixes, 47 products) — rebuilt, validated, re-uploaded to Supabase Storage, `DB_VERSION` bumped 2026-07-20-1 → 2026-07-22-1 so devices with an existing copy re-download. (2) First-name capture (this session's earlier work) is new data collection never disclosed — added "First name" to `web/privacy.html`'s Account Information list; **ASC Privacy Label still needs the founder to add a Name data type** (can't be done from code). (3) Account-deletion failure alert and two Open Food Facts User-Agent headers pointed at a dead `support@healthychoices.app` address (that domain was never real) — fixed to `jamesadventuremarketing@gmail.com` in `ProfileScreen.js`, `ScannerScreen.js`, `ProductSearchScreen.js`. (4) In-app legal links (Paywall, Onboarding, AuthScreen) pointed at `shelfexpose.netlify.app`, which still had the stale "Shelf Expose" branding pre-dating today's site rename — repointed to `shelfexpose.app` (the freshly rebranded live site). | Founder: "anything else before we submit a build?" — ran the project's own `submission-preflight` skill rather than guess | 968/968 tests pass. Two real native modules confirmed since the last known submitted commit (`@react-native-community/slider`, `expo-image`) — this build cannot ship as an OTA update, needs a real EAS build (expected, not a blocker). Not yet committed — awaiting founder go-ahead |
| 2026-07-21 | **App Store Connect metadata finalized**: Subtitle `Know Everything In Your Food` (28/30 chars — "About" was 31, over the limit, "In" fixes it), Keywords `ingredient,checker,nutrition,additives,grocery,label,ultra,processed,score,clean,dyes,oils,companies` (Chad-researched, exactly 100/100 chars, no overlap with name/subtitle words). **Home-screen greeting name capture built**: `userName` state in `HomeScreen.js` had been dead code (declared, never set — every user saw a bare "Good morning" with no name, stacked right above the "Food Exposé" title, reading like one line). Fixed via new `src/utils/userName.js` (`getFirstName`, checks `first_name`/`given_name`/`full_name`/`name` in that order) wired to `useAuth()`. Capture added for all 3 sign-in paths: email signup gets a new required First Name field written to `user_metadata.first_name`; Apple Sign-In's one-time `FULL_NAME` grant (previously requested but discarded) is now persisted via `supabase.auth.updateUser` right after first authorization, since Apple never sends it again; Google's OAuth already populates `user_metadata` from Google's profile with no code change needed | Founder: wants the name captured "in all ways possible" now that the greeting bug surfaced; subtitle/keywords are the founder's + Chad's final picks for the 1.2.0 ASC submission | 968/968 tests pass. Not yet committed. ASC metadata (subtitle/keywords) must be pasted in manually — lives outside the repo, not something committable |
| 2026-07-21 | Version bumped 1.0.2 → **1.2.0** in `app.json`/`package.json` for the next submission (carries the rename, icon, Specs transparency fix, scanner contrast fix) | Founder call: App Store Connect already shows 1.1.0 live (a version the repo's git history never recorded — real drift between ASC and the repo), so the next version must be >1.1.0. Chose 1.2.0 over a 1.1.1 patch bump given the scope riding this release (rename + icon + onboarding/paywall overhaul, not just a fix batch) | Founder-confirmed. Build number is EAS `autoIncrement`, not tracked here |
| 2026-07-21 | **App renamed "Shelf Exposé" → "Food Exposé: Barcode Scanner"; app icon replaced with mascot Specs (waving pose, brand green `#1D9E75`).** Rename applied to `app.json` (`name`, camera-permission strings), all live in-app screens (Home, Auth, Scanner, Profile, ProductScore, CompanyList), `scorer.js`'s packaging-note string, the 3 legal pages + marketing `web/index.html`, and this project's current-state docs (CLAUDE.md, company-map.md, notification-copy-bank.md). Icon: extracted the true wave-peak frame from the already-approved `specs-wave` animation (`assets/mascot/transparent/specs-wave.webm`, frame 0.50s — found by measuring arm-extension across all 32 frames, not guessed) via a real alpha decode (ffmpeg's default path silently flattened VP9 alpha to opaque — required forcing `-c:v libvpx-vp9`), composited onto the brand-green `#1D9E75` full-bleed for `assets/icon.png` (opaque, iOS) and a safe-zone-sized transparent version for `assets/adaptive-icon.png` (Android) | Founder-directed rebrand + distinctiveness upgrade — a custom named mascot is a stronger answer to the earlier 4.3(a) "stock icon" rejection than the old generic shelf+magnifier mark. Reused the existing approved animation asset rather than a fresh AI generation, to avoid any character drift off the locked reference | 968/968 tests pass post-rename. **Explicitly NOT changed** (technical identifiers, not display text — changing these would break EAS/Apple/domain linkage): `slug` (`healthychoices`), `ios.bundleIdentifier`/`android.package` (`com.jamesadventure.healthychoices`), `scheme`, EAS `projectId`, RevenueCat entitlement id, `shelfexpose.app`/`.netlify.app` domains. **Still needs founder action outside this repo**: App Store Connect listing name/subtitle, RevenueCat product/offering display names ("Shelf Exposé Pro"), Supabase auth email templates (confirmation/reset emails aren't in this codebase), and the separately-hosted Higgsfield marketing site (fierce-pine-854.higgsfield.app is its own codebase, not `web/` in this repo) still says "Shelf Exposé." Historical decision-log/priorities/memory entries describing past events under the old name were deliberately left unedited (accurate record of what was true at the time), not overlooked. Rides the next `eas build`/App Store Connect metadata update, not committed to git yet — awaiting founder review |
| 2026-07-20 | **Onboarding/paywall batch: plan-mode designed, Hadrian-built, independently Opus-reviewed — 5 real findings, 2 fixed as genuine bugs, 3 confirmed non-issues or pre-existing.** Founder gave a large freeform spec (welcome page copy, scary-stats page, PepsiCo rebuild, new goal/challenge/commitment steps with a real slider, paywall rename+repricing, exit-offer funnel). Plan mode + an Explore pass mapped every anchor before building; Hadrian executed in 3 checkpointed waves; a full 4-agent `/political-analysis` pipeline sourced the new PepsiCo GMO-labeling issue (researcher→fact-checker→legal-writer→legal-reviewer, one double-count fix applied by the reviewer before it shipped); a separate Opus reviewer (creator≠reviewer doctrine) caught a real never-fabricate violation (a hand-typed "270+" companies claim when the real count was 268) and a real requirement gap (the exit-offer screen wasn't wired into ~8 in-app paywall triggers, only onboarding). Also independently found and fixed, outside any review pass: a `COMPANY_SPOTLIGHT` shadow-data object in onboarding whose PepsiCo numbers had silently drifted from the real `companies.js` record ($6.2M shown vs. real $3.9M lobbying). | Founder: large scope, high stakes (paywall = revenue, PepsiCo copy = legal exposure, stats page = never-fabricate risk) — warranted the full plan→build→independent-review pipeline rather than a single pass | 288/288 tests green, `expo export` bundle-resolution smoke clean, all grep gates (fabricated stats, stale "Pro" strings, ungated share) pass. Uncommitted pending founder review. Full detail: priorities.md's "Onboarding redesign + Paywall overhaul" entry, `.claude/plans/joyful-waddling-karp.md`, `src/data/research/pepsico_gmo_labeling_*` |
| 2026-07-13 | **Live incident: vandalized OpenFoodFacts listing surfaced a false claim as the app's own voice; root-caused and patched (commit `2155004`).** Founder scanned a real Justin's Peanut Butter Spread; the app fell back to the LIVE OFF API (curated `products.js` entry for the same barcode is clean — real ingredients, correctly attributes the 2017 acquisition to Hormel). The live OFF listing had been vandalized: a first-person narrative injected into `ingredients_text`, including a FALSE claim that "Justin sold out to Kraft or Nestle for hundreds of millions." Two compounding bugs: (1) the parser had no defense against full-sentence injection into a public, user-editable data source; (2) worse, `generateScoreExplanation` (the "Shelf Exposé Says" box) is a template that quotes whatever's in the flagged-ingredients list verbatim — so the vandalism got presented as the APP'S OWN editorial claim about a real business transaction, not just a mis-parsed ingredient row. Verified against the already-committed-but-unshipped P0 fix (2026-07-13 earlier commits `6e8fcf5`/`396e1a8`): 3 of the 4 injected phrases were already caught by the plausibility gate's word-count check, including both narrative sentences carrying the false claim. One gap found and closed same-session: a short phrase ("Local Boulder Colorado Nut Butter") was under the gate's word-count threshold. Added `/^local\s/i` to the advisory-phrase blocklist — sourcing/marketing descriptors never open a real ingredient declaration. Regression test added from the exact incident text. 826/826 tests green. | Founder screenshot flagged it directly ("what in the world happened here") | Fix ships with the same next `eas build` as the rest of P0 — this incident is now impossible to reproduce with the committed code, confirmed by testing the exact injected strings against the shipped-but-unbuilt gate before writing the patch |
| 2026-07-13 | **App ingredient data-quality pack: two commits, both reviewed and merged.** Founder's live-scan screenshots (Coca-Cola/generic snack products) showed 4 distinct bugs, each root-caused via a parallel Explore investigation before any code was touched. Root causes: the app has TWO independent ingredient parsers (`productParser.js` runtime + `ingest-products.js` build-time) and neither ever deduped or sanity-checked a token; `analyzeIngredients` in `scorer.js` emitted every unmatched string as a visible "Unknown" row unconditionally; `ingredientCache.js`'s 3,528 explanation strings were written for a developer audience, not a shopper. **Commit `6e8fcf5`**: new `src/utils/ingredientNormalizer.js`, shared by both parsers — normalized-name dedup (kills "Canola Oil" appearing twice), conservative parenthetical oil-disclosure resolution (only collapses "vegetable oil (X, Y)" to "X, Y" when every item inside the parens is a recognized specific oil; founder-review caught the builder's version wasn't appending " oil" to bare disclosed names like "canola", patched before commit), an expanded advisory-phrase blocklist (catches "Added To Preserve Freshness" and similar, which slipped through the old to/for/as-anchored filter), and a plausibility gate with a bounded Levenshtein fuzzy-rescue (OCR typos like "sgybean" silently correct to "soybean"; true garbage like "Balle Creak" is hidden entirely behind a one-line "N unreadable label entries hidden" UI note instead of rendering as a fake ingredient). Builder (Hadrian) also caught and fixed a real bug in the offline catalog-builder's VM-sandbox bundling that the new normalizer dependency broke, and self-disclosed an accidental live-network side effect from testing (reverted the tracked state file; gitignored artifacts left as harmless dedup-on-merge). 824/824 tests green (801 existing + new fixtures built directly from the founder's screenshots). **Commit `396e1a8`**: all 318 `ingredientCache.js` explanations containing internal dev-speak ("Parser phrase for…", "follows the X risk", "Purpose clause for…", "Class phrase for…", "Quantity fragment…" — more variants than the ~140 originally estimated) rewritten to plain 8th-grade language, sourced where a health claim appears (FDA 21 CFR cites, IARC classifications), zero causation language, risk/category/keys untouched. Founder-review's own independent re-grep caught 6 entries the cleanup pass's sweep missed (all "Quantity fragment" variants for salt/water/fat percentage disclosures) and hand-fixed them before commit. **Scope correction from the original plan**: this does NOT ship over-the-air — the 2026-07-12 products.db rebuild made the catalog manual-only, so the screenshotted garbage traces to the LIVE-SCAN code path, not the downloadable DB. Rides the founder's next `eas build`, not a hot patch. | Founder scanned real products post-launch and found the bugs live; asked for root-cause analysis before any fix (2 parallel Explore agents dispatched, findings presented before planning) | Both commits merged to main; 824/824 tests green after each; independent review (not just trusting agent self-reports) caught one missing suffix rule and 6 missed rewrites before shipping either commit |
| 2026-07-13 | **shelfexpose.app DNS "cutover" resolved as a Cloudflare redirect, not a true host swap.** Higgsfield websites have no custom-domain feature (verified: no CLI flag, no platform doc mention) — sites are permanently `*.higgsfield.app` only. Founder's domain DNS lives on Cloudflare; before this fix it was silently serving the OLD pre-rebuild Netlify site. Built 2 Cloudflare Redirect Rules (apex + www → `fierce-pine-854.higgsfield.app`, 301, query-string preserved). Two non-obvious gotchas found via live testing, not docs: (1) the DNS records were "DNS only" (gray cloud) so Cloudflare Rules never fired at all — had to flip both to Proxied; (2) Redirect Rules default to "Static" type, which sends a `concat(...)` expression to the browser as a literal string unless the Type dropdown is explicitly switched to "Dynamic" (field relabels URL→Expression as confirmation). No MX records on the domain, so email was never at risk. | Founder asked to finish the DNS cutover flagged as blocked in the prior session; investigation found custom domains are a Higgsfield platform gap, not a config error | Live and verified end-to-end (curl -L lands on the real site; query strings survive). If a real branded-URL host is ever wanted, the site's React/TanStack Start code is a portable git repo, not locked to Higgsfield's CI |
| 2026-07-12 | **Marketing site: seamless-marble fix shipped (sixth deploy today, commit d717d9c, founder-designed approach, plan-mode approved).** Prior fix had duplicated the produce photo (scrub overlay + Reward section = veggie bag twice) and the book-matched tile still hard-cut against the photo. New model per founder's own proposal: the produce photo appears ONCE (Reward), its top/bottom edges feather via CSS mask (140px buffers) into a FLAT tone `--color-marble: #edefee` sampled (PIL average) from the photo's own marble, and that tone is the continuous background of the scrub handoff, Reward, Waitlist, and Footer (hairline border dropped too — borders are hard transitions). marble-surface.jpg deleted. | Founder screenshots showed the double bag + remaining hard transitions; his instinct (flat sampled color + buffer zones) was the architecturally correct fix | Live; verified: 1 photo instance, all four Act 2 sections computed to identical rgb(237,239,238), mask active, no overflow; gates passed |
| 2026-07-12 | **Marketing site: formatting pass 3 shipped (fifth deploy today, commit fc4ac9b).** Tabs on the case files removed entirely (founder: "files in real life don't have tabs"); file typography scaled to match the +20% file size (spine names 11→14px, dossier type up a full step); cards raised to 576px so all 10 dossiers fit with NO inner scrollbar, verified per-dossier; PepsiCo's Doritos exhibit REMOVED (was the one dossier that couldn't fit, and the founder had flagged PepsiCo as oddly "the only one with a product"; asset deleted, in git history if wanted back). Marble seam fixed at the root: the scrub now dissolves into the EXACT Reward photo (pixel-identical at unpin, zero seam) and Waitlist/Footer use a book-matched, native-scale marble tile built from the photo's own marble region with the white veil removed (the old narrow crop rendered 2.3x zoomed + veiled = the "different sheen" the founder saw). | Founder's third live review with screenshots; his message cut off mid-sentence ("also the...") — awaiting the rest | Live at fierce-pine-854.higgsfield.app; gates passed; note for future sessions: the Browser pane freezes not just rAF but React HYDRATION itself when hidden, and `vite preview` serves a stale in-memory SSR bundle after rebuilds (restart it); unhydrated SSR DOM still allows content-height measurement |
| 2026-07-12 | **Marketing site: formatting pass 2 shipped (fourth deploy today, commit 71d814a).** Founder-supplied hero background swapped in (aged "Product Investigation Report" desk scene with FICTIONAL brands: Golden Harvest Cereal / National Food Corp / Consolidated Brands — zero brand-risk; sourced from his Downloads, optimized 240KB jpg); hero contrast fixed (center-weighted dark scrim + paper-toned subtext with text shadow + "Keep believing the label" ghost CTA given a real dark fill) after founder flagged white-on-white; Evidence Archive file tabs all centered (staggered tabs "looked weird"); files +20% (spine 72/open 576/rail 528, row now full-bleed via 100vw breakout since it exceeds max-w-6xl); dossier inner scrollbars removed (all 10 verified to fit statically, PepsiCo tallest at exactly-fit); Reward headline "Walk the aisle like you already know." → "Shop confidently."; marble continuity: marble-only crop of the produce photo (30KB) now backs Waitlist + Footer, and ForkScrub cross-fades the final produce frame into that marble over the scrub's last 6% so Act 2 reads as one continuous scene; Scan/Grade/Follow-the-money cutouts +25% (180/220px). | Founder's second live review, eight specific directives, plan-mode approved | Live at fierce-pine-854.higgsfield.app; all gates passed; verified via local build + JS probes (tab positions, cell widths, no page overflow at 1280/375, dossier fit, marble serving, scrub overlay screenshot-safe at opacity 0) |
| 2026-07-12 | **Marketing site: Evidence Archive v2 + formatting pass shipped (two deploys same day, founder-directed iteration).** v2: per-file size/angle variance killed (founder: "horrible, I don't know why I suggested it") for uniform Foliom-style spines with one shared shelf lean; hover now opens the dossier IN PLACE inside the rail (detail-panel-below deleted); content reordered wrongdoing-first, brands last; company set re-ranked to the DB's top-10 lobbying spenders with founder overrides (keep Tyson + Coca-Cola, drop Kroger + Abbott): Amazon $21.1M, Walmart, AB InBev, Bayer, Cargill, ADM, Nestlé, PepsiCo, Tyson, Coca-Cola. Cargill/ADM render their `brandsNote` (no consumer brands) instead of fake pills. Formatting pass: hero background replaced with a Higgsfield-generated investigation-desk plate (open case files, unbranded product photos, red string, top-left key light per founder's lighting note; old corkboard asset deleted) + radial top-left-light overlay; "Three steps" red string now MEASURED to the true pin centers every scroll tick (root cause of the detached pins: cards drift ±10-18px with parallax while the old string was a static path) with an 800ms tail for GSAP scrub settle; ForkScrub now cross-fades between adjacent frames at fractional positions (source video is only 5s/24fps ≈ 122 real frames, so interpolation beats re-extraction), scroll distance tightened 280%→190% (mobile 150%→120%), scrub 0.75→1; "Now you see everything" heading gated to appear only at the white flood (founder: it made no sense over the noir scene). Sonnet builder did v2 until it hit its session limit mid-run; Fable finished, reviewed, verified (all claims trace to companies.js; string-to-pin distance ≤2px measured live), and shipped both deploys per founder's standing "deploy when done." | Founder reviewed the live v1 rail against his Foliom reference and gave specific corrections; separately flagged hero background, pin misalignment, and scrub bumpiness | Both live at fierce-pine-854.higgsfield.app (commits d9f6efc, 0ab4e3b). All gates passed both times. Browser-pane caveat noted: hidden-tab rAF freeze limits animation verification to JS probes + geometry replication |
| 2026-07-12 | **Marketing site: "Evidence Archive" case-file rail shipped** (replaces the static Doritos "Exhibit A" section). Founder-directed design from 5 reference images: Foliom-style 3D fan of 10 manila folders, company names legible on every tab, driven by horizontal MOUSE position (rAF-lerped focus, not scroll), click opens a per-company dossier (revenue, lobbying, donations + R/D split bar, brand pills, one flagship documented issue with source); Doritos exhibit preserved as PepsiCo's Exhibit 01. All data field-faithful from `companies.js` (traceability-verified per claim before ship); em-dash/markdown bans applied; SSR static-fan fallback + keyboard (roving tabindex, arrows/Enter/Esc) + mobile scroll-snap + reduced-motion paths. Sonnet builder agent wrote it; Fable reviewed the full diff, verified claim traceability, and patched the rAF loop to self-stop on convergence. Same deploy: **fixed fabricated "7,500+ products analyzed" → "1,000+"** (real catalog: 1,045; never-fabricate rule). | Founder: iterate the existing site; the single static Doritos card was the weakest section and he wanted the immersive "files on Nestlé/Pepsi/Coke/Walmart" feel | Live at fierce-pine-854.higgsfield.app (deploy 2026-07-12; typecheck/build/qa:fill --strict all passed; live HTML verified: 10 folders + corrected stat). Bun installed on this machine for local gates (was missing). Browser-pane note: hidden-tab rAF throttling makes screenshots/animation untestable in the pane — verify interaction via JS probes or a visible browser |
| 2026-07-12 | **Cicero X-post doctrine overhauled after the founder reviewed the August batch and hand-rewrote it** (`marketing/2026-07/Founder Written Linkedin and X posts.txt` + inline critiques on Posts 16–20 of `x-2026-08.md`). New rules in `cicero.md`: (1) hook = the scariest TRUE fact in the fewest words ("Soda + sunlight = carcinogen"), never a definition/setup; (2) THREE alternate hooks per X post, founder picks — AI still lacks human taste on hooks; (3) no markdown/bolding inside post copy (X renders literal asterisks); (4) em-dashes banned in ALL customer-facing copy (AI tell); (5) "bell on"/"alerts on" CTA variants permanently banned — CTAs one plain line or none; (6) no hedged "some/certain" product lists — name products or cut; (7) content angles reranked: company scandals w/ receipts, one-giant-fact series seeds (palm oil/deforestation class), personal stories, score reveals, blunt questions; ingredient-FUNCTION explainers ("it's an emulsifier") ruled non-converting and banned as a post's point. Hook-playbook's old "tension never fear" guardrail superseded: accuracy is the only rail, documented danger words belong IN the hook. | Founder verdict on the August drafts: hooks weren't hooks (wordy, academic), CTAs overthought, formatting screamed AI, and the batch missed the highest-converting material entirely. His rewrites + the one post he praised (avocado-oil car bumper: hook → story → body → fun CTA) are the new calibration source, ranked above the older `x-posts-growth-batch.md` exemplar (which got a header notice: copy energy, not formatting/CTAs). | Doctrine live in `cicero.md` + `hook-playbook.md`; pilot redraft of the trashed August posts run same session — founder verdict "way better," praised format variety. Three more rules verified + encoded same day: every harm/deception claim names its company/product in-post; big LIVE issues only (2006 benzene survey post killed as moot — resolved 20-year-old stories are trivia even when accurate); never promise unshipped app features in CTAs (Cicero had invented "applies the strictest state standard" etc. — app mentions limited to real capabilities). Homework-style engagement questions ruled "meh"; blunt instant-opinion questions only. **Third correction pass (founder audit of the ruleset itself) caught a real bug**: cicero.md's "pre-launch framing" rule was stale — the app shipped 2026-07-06 but the doctrine still said "I'm building Shelf Exposé," and that had already leaked "being built"/"waitlist" language into the drafted August batch (fixed live in x-2026-08.md, linkedin-2026-08.md, video-2026-08.md, calendar-2026-08.csv). Also flipped: journal-naming rule now defaults to USING real names/specifics (only flag genuine legal/financial risk, don't default to silence); medical-claims rule now explicitly allows "linked to/correlated with" for sourced claims (was blocking true claims like "processed oils linked to heart disease" by requiring pure regulatory-fact framing) |
| 2026-07-12 | **Brands tab fixed for the 5 companies with no real consumer brands** (ADM, Cargill, Bunge, Ingredion, TreeHouse Foods) — `subsidiaries` was storing a descriptive sentence ("ADM (ingredient supplier — HFCS, corn starch...)") instead of a real brand list, rendered oddly inside a tag-pill UI meant for short names. | Founder screenshot showed the malformed pill on ADM's profile. Not a code bug — `company.subsidiaries` was being asked to hold two different kinds of data (real brand lists vs. "no consumer brands, here's what they actually make" notes) with no way for the UI to tell them apart. | `companies.js`: those 5 records now have `subsidiaries: []` (accurate) + a new `brandsNote` field with the descriptive text. `CompanyProfileScreen.js`'s Brands tab renders the pill grid when subsidiaries exist, a plain descriptive-text card when only `brandsNote` exists, and the existing empty state otherwise. 805/805 tests passing; `products.db` unaffected (subsidiaries/brandsNote aren't in the SQLite schema — the Brands tab reads `companies.js` directly). |
| 2026-07-12 | **products.db production outage closed out**: founder uploaded the rebuilt 0.93MB `products.db` to a new public Supabase Storage bucket (`Catalog`) via the dashboard and provided the public URL + a service-role key (renamed from a space-containing `.env` key to `SUPABASE_SERVICE_ROLE_KEY` so it's actually usable by a script). | Closes the loop on the Sentry `Cannot find module` outage and the "which host" decision from earlier the same day — the file was small enough (post size-fix) that no Supabase tier upgrade was needed. | `REMOTE_DB_URL` in `productStore.js` now points at the real, verified-live URL (confirmed via `curl -I`, Content-Length matches exactly). Built `scripts/upload-products-db.js` — reusable, reads the service-role key from `.env`, uploads with `upsert: true` — and test-ran it (safe: same file, same destination) to confirm it works before relying on it for future catalog updates. 805/805 tests passing. |
| 2026-07-12 | **`products.db` rebuilt manual-only, dropping `products_generated.json`'s ~136k OFF bulk rows** (`scripts/build-products-sqlite.js`, `scripts/validate-products-sqlite.js`). | Founder: Supabase's free tier caps uploads at 50MB and didn't want to upgrade just to host this one file. Investigated why the DB was 161MB in the first place rather than just working around the cap: `products_generated.json` was never supposed to reach production at all (excluded from EAS builds via `.easignore`, silently no-op'd behind a `require()` try/catch in `products.js` — the app's real production `PRODUCT_DB` has always been manual-only). Folding those ~136k rows into `products.db` was scope creep in the SQLite build script, not an intentional feature. | **161MB → 0.93MB**, still all 1,045 curated products, `validate-products-sqlite.js` rewritten for the new manual-only expectation and full PASS (summary counts match the founder's own "before" screenshot exactly, e.g. Beverages 49 picks — confirming this is what was actually working pre-outage, not a regression). Comfortably clears Supabase's 50MB free-tier cap with no upgrade, no compression, no chunking. Also fixed a second bug uncovered along the way: both SQLite scripts' `loadHealthyCategories()` VM shims still referenced the old `categoryAccents` export the same-day gradient-color change (see below) had renamed to `buildCategoryAccents` — both scripts now stub the new name. `assets/db/products.db` is gitignored (build artifact); founder still needs to upload it to Supabase Storage and hand back the public URL for `REMOTE_DB_URL`. |
| 2026-07-12 | **Home category-tile colors changed from a 3-tone round-robin to a computed pair-based gradient** (`src/constants/colors.js`'s new `buildCategoryAccents()`, `src/data/healthyCategories.js`). | Founder found the old cycling green/deep-green/tan-every-3-tiles pattern visually unappealing once the SQLite outage fix restored the category grid. Wanted consecutive category pairs to share a shade, progressing light green → deep dark green, then restarting light → dark brown, with the green/brown split computed from category count (not hardcoded) so it stays balanced as categories are added. Confirmed via a rendered widget preview, then a plain-text hex table (widget didn't render on the founder's client) before implementing — founder confirmed "that gradient scale works great" against the real 25-category output. | 805/805 tests passing (10 new tests in `src/constants/__tests__/colors.test.js` covering pairing, endpoints, the light-restart at the green/brown boundary, and split math at several category counts). Live trace of `HEALTHY_CATEGORIES` matches the approved table exactly. |
| 2026-07-12 | **`products.db` init rewritten: runtime `FileSystem.downloadAsync` instead of Metro `require()`-bundled asset** (`src/data/productStore.js`, `metro.config.js` deleted). | Sentry reported `metroRequire: Cannot find module` from `initProductStore()` in production; founder then independently reported symptoms confirming it live — every company's Products tab showed "0 products," and Home's "Eat better by category" section was empty. Traced both to the same cause: `_db` never opens because Metro can't reliably bundle the 161MB `.db` as a require()'d asset (same class of risk the team had already flagged for a 221MB JSON file in `.easignore`). Bundling it also meant catalog updates never reached devices that already had a local copy — a second latent bug, fixed for free by the same change via a `DB_VERSION` marker file that forces re-download when the catalog is rebuilt. | Code + tests done (795/795 passing, `metro.config.js` removed as dead config). **Not yet fixable in production**: `REMOTE_DB_URL` in `productStore.js` is a placeholder — needs the founder to host `products.db` somewhere with a stable public HTTPS URL (Supabase Storage bucket or similar; no service-role key or bucket exists yet, so this needs a founder decision on where/how, not something to provision unilaterally) and paste the real URL in before this ships in a build. |
|---|---|---|---|
| 2026-07-11 | **Full 10-batch Octavius product push complete — catalog crosses 1,000.** All 10 category batches (Kids Lunch, Deli Meat, Peanut Butter, Chips, Crackers, Eggs, Hot Cereal, Granola, Pasta Sauce & Cooking Sauces, Coffee & Tea) run end-to-end through script-fetch → Octavius Sonnet research → Opus review → script-merge, spread across two sessions with several session-limit interruptions along the way (all recovered cleanly — work always survived on disk, checked and validated before continuing). | Founder: "run the octavius batch on those 187 candidates" (candidates generated by Chad via `codex exec`, see the 2026-07-09 entry below), then "keep going" through both sessions | **892 → 1,045 products.** 153 of 187 candidates actually merged; the rest correctly held back as `could_not_verify` or excluded duplicates rather than guessed. The review stage earned its keep repeatedly: caught 3 fabricated-ingredient rejects, a fabricated "Gluten Free Certified" claim (real celiac-safety risk, not cosmetic), a sodium-unit bug (OFF reports grams, not mg) recurring in nearly every batch, and a batch (Eggs) where literally every Stage-1 "resolved" record was wrong-product and had to be re-researched from scratch. Independently, reviewers also found and fixed 5 mis-owned EXISTING company records (outside the new batches' scope, but well-evidenced so fixed on the spot): Peter Pan (Conagra → Post Holdings, sold 2021), a duplicate Campbell/Campbell-Soup company record consolidated, Classico (Mizkan → Kraft Heinz), Victoria Fine Foods (Rienzi & Sons → B&G Foods, acquired 2016), and 4 Starbucks retail-coffee entries (JDE Peet's → Nestlé, per the 2018 Global Coffee Alliance — JDE Peet's has no real Starbucks relationship). Also found and fixed 2 real bugs in the merge script itself: a stale insertion anchor left over from the SQLite re-architecture that was silently corrupting `products.js` on every merge, and a quote-serializer that blindly converted every `"` to `'`, breaking on any apostrophe in a name/brand. 794/794 tests passing throughout; `assets/db/products.db` rebuilt and validated after every single merge. Photo coverage lagged behind (~40 recovered via a leading-zero barcode-normalization fix, most of the 153 new products still lack a photo) — flagged as the next real gap in priorities.md P2, not solved here |
|---|---|---|---|
| 2026-07-10 | Founder ran and submitted a new build to Apple (night of 2026-07-09→10), carrying the full accumulated 2026-07-08/09 session work: scoring ceiling + retiered certifications, search resilience fix, SecureStore `keychainAccessible` hardening (closes Sentry #4), the earlier Sentry ANR fix, 76 new products (968 total, up from 892), and the "1,000+" home badge copy | Founder-run build/submit — Claude never runs `eas build`/`eas submit` per standing policy; this closes out a large batch of committed-but-unbuilt work in one shipment | Auto-incremented build number (off prior TestFlight build 31, per `eas.json`'s `autoIncrement: true`), version string unchanged at 1.0.0. Awaiting Apple review. `submission-preflight` wasn't run ahead of this one (already submitted by the time it came up) — flagged to run proactively before the next submission |
|---|---|---|---|
| 2026-07-09 | Certification bonus retiered (`src/utils/scorer.js`, same session as the scoring-ceiling change): every cert string used to get a flat +3 regardless of what it actually verifies. Now four tiers by real substantiation — Tier A/+3 (USDA Organic, Non-GMO Project Verified, Heart-Check: independently verify the ingredient/additive profile), Tier B/+2 (Certified Gluten-Free/GFCO, Certified Vegan, Nut-Free Certified, Kosher, Halal: real verification, scoped to a specific dietary/allergen need), Tier C/+1 (Fair Trade, Certified Humane, Rainforest Alliance, B Corp, California Olive Oil Council: real certification, about ethics/sourcing not this product's health profile), Tier D/+0 (Keto/Paleo/Whole30/Made in USA: real programs, but dietary-pattern or country-of-origin claims, not independently verified safety/quality standards). Unrecognized cert strings default to +1, not the old blind +3. Total capped at +8 | Founder: "let's do the certification research right now, so that ships with next build" — following up on the claims-audit finding that "Made in USA" got the same score bonus as USDA Organic | 314/314 scorer tests passing (5 new tests added, verifying the tiering explicitly and the cap). Catalog-wide: 116 of 285 certified products see their bonus drop, 0 increase — consistent with "make scoring stricter and more honest." No EWG-Verified products exist in the catalog yet, but the tier (A) is ready. `assets/db/products.db` rebuilt a second time same day and validated clean with this change baked in — ready to ship with the next build |
| 2026-07-09 | Product-candidate research (the Chad brief from earlier today) run directly via `codex exec` instead of the founder pasting into ChatGPT | Founder: "you can prompt chad, stop asking me to prompt chad." The programmatic `codex exec` lane has been the documented standing default since 2026-07-05 for anything that fits a self-contained prompt — this task qualified, it just hadn't been used for product research specifically before | 187 real candidate rows produced (`candidates-2026-07-09.csv`), covering all 10 target under-filled categories (16–21 each). Every barcode left blank — gpt-5.5 chose not to guess any, exactly per the no-fabrication instruction. Validated: 0 duplicates against existing `products.js` catalog entries, spot-checked realistic. Ready for Octavius Stage 0 whenever the founder wants to run the batch (not auto-triggered — running the full verification pipeline is a separate agent-cost call) |
| 2026-07-09 | SecureStore hardened against background keychain-write failures (`src/utils/supabase.js`): added `keychainAccessible: SecureStore.AFTER_FIRST_UNLOCK` to all three adapter methods (Expo's default, `WHEN_UNLOCKED`, requires the device to be unlocked at the exact moment of access) | Founder: "sentry notifications is working we have gotten 3" — traced all 3 GitHub auto-filed issues: #2 test issue, #3 the ANR already fixed same day, #4 (`setValueWithKeyAsync... User interaction is not allowed`) a real, distinct iOS Keychain error. Confirmed it's the same failure mode `fcd8ff5` "Fix auth refresh while backgrounded" (already on `main`, not yet shipped in a build) targets by stopping Supabase's auto-refresh timer while backgrounded — but that alone doesn't close the race where a refresh already in flight completes its keychain write after the device locks | Committed. Belt-and-suspenders alongside `fcd8ff5`: the AppState toggle prevents most background refreshes from firing at all; this accessibility change means any write that does land while locked no longer fails. Babel-parse clean, 791/791 tests still passing (no dedicated SecureStore test exists — native module, not unit-testable without a device) |
|---|---|---|---|
| 2026-07-09 | **Scoring methodology overhauled** (`src/utils/scorer.js`): a literal 100 is now reserved for a genuinely raw, single-ingredient whole food in packaging that's been VERIFIED (not just assumed) clean — new `isRawWholeFood` gate. Everything else (pasteurized, roasted, canned, juiced, or with unresearched packaging) caps at a new `PROCESSED_CLEAN_CEILING` of 96, intersected with the existing `upfCeiling` marker-load curve. Missing packaging data now defaults to a small `UNRESEARCHED_PACKAGING_PENALTY` (2 pts) instead of silently skipping the packaging check entirely | Founder: "Tropicana should not be getting a one hundred score... nothing that's stored in plastic should be getting a hundred percent score... the only things that should be getting one hundred are zero processed foods, entirely whole foods... even the absolute cleanest product has been processed... or the container is bad." Founder locked the exact mechanism via 2 clarifying questions: hard cap by format (not just a bigger packaging deduction) + assume-plastic-until-proven-otherwise for missing data | Verified: Tropicana Pure Premium OJ (single ingredient "pure pasteurized orange juice", no packaging data) went 100→96. Catalog-wide: **0 of 136,491 products now reach literal 100** (intentional — verified-clean packaging data barely exists yet, matching the founder's "even the cleanest product" framing). 791/791 tests passing (2 new tests added for the raw/processed split, 5 existing hardcoded-100 assertions updated to reflect the new ceiling with full before/after math in comments). `assets/db/products.db` rebuilt (14.8s, 136,491 rows) and validated clean with the new scoring precomputed. Not yet done: the certification bonus still gives every cert string (including non-health ones like "Made in USA") the same flat +3 — flagged in priorities.md P2 as a follow-up, deliberately not folded into this change to avoid scope creep on an already-large rewrite |
| 2026-07-09 | Search resilience fixed (`ProductSearchScreen.js`): added an 8s `AbortController` timeout to both OFF fetch calls (Search-a-licious primary + legacy fallback) and a visible "showing local matches only — live web search is unavailable" notice when the remote fetch fails but curated local hits still exist | Founder: "search feature really doesn't search, instant Ramen gets one result." Live-traced the actual failure mode: replicated the exact app query against OFF's real API (got 24 good hits with nutrition data that would pass every app-side filter), confirming the query/filter pipeline itself is correct. The catalog only has 1 local ramen product (Maruchan), so if the remote fetch also silently fails (RN's fetch has NO default timeout, unlike browsers — a hung request could wait indefinitely), the user sees exactly 1 result with zero indication live search ever ran, since the existing error banner only shows when local results are ALSO empty | Fix verified via Babel parse + full test suite (791/791 still green — no dedicated ProductSearchScreen test suite exists yet, flagged as a gap). Needs a real-device retest on a throttled/flaky connection to fully confirm the original symptom is gone |
| 2026-07-09 | Photo coverage: ran the free local `product-photo-lookup` skill's Phase 1 script (OFF bulk fetch) against all 539 products missing an image; merged the 101 resolved URLs directly into `products.js` via a scoped text-patch (matched `'<barcode>': {` blocks, replaced only `image: null,` — verified 0 collateral changes via Babel parse + product count check) | Founder: "let's get all photos uploaded for all thousand products... assign as much work as possible to chad." Local scripts are the cheapest tier (cheaper than Chad, zero session cost) and are exactly what the `product-photo-lookup` skill's Phase 1 is for — ran it directly rather than routing to Chad, which doesn't fit this specific mechanical HTTP-fetch task | Coverage: 353/892 (40%) → 454/892 (51%), zero session cost, zero fabrication risk (only real OFF-hosted image URLs merged, temp CSV/JSON/log files cleaned up after). Remaining 438 need Phase 2 (UPCItemDB/USDA, still scriptable) then Phase 3 (retailer-page lookups via the `octavius` agent, small batches only) — sized in priorities.md P2, not run today |
| 2026-07-09 | Wrote a Chad task-board brief (`context/chad-tasks.md`) for product-candidate research targeting the DB's thinnest categories (Kids Lunch, Deli Meat, Peanut Butter, Chips, Crackers, Eggs, Hot Cereal — all under 20 products), aimed at pushing the 892-product catalog past 1,000 | Founder: "let's get over a thousand products... assign as much work as possible to chad." Chad's actual lane (per `company-map.md`/`efficiency.md`) is fuzzy research/classification work, not structured schema+photo+company pipeline work (that's Octavius's job, already script-first and cheap at ~2 agents/batch) — routed the ONE genuinely Chad-shaped piece (finding real candidate products/barcodes) there, rather than force-fitting Chad onto the whole pipeline | Brief drafted, not yet sent (founder pastes into ChatGPT per the standing protocol). Requests 150-200 real candidates (barcode may be blank, never fabricated) as a CSV, to be verified/enriched through the full Octavius Stage 0-4 pipeline before anything merges — nothing from this brief lands in the app un-verified |
| 2026-07-09 | Marketing-claims-vs-app-reality audit, founder-requested ("audit our socials to check that our app actually flags what we say it flags"): (1) banned-ingredient flagging is REAL but PASSIVE (shown when you scan a product containing a documented FDA-revoked/EU-banned ingredient, per `ingredients.js`) — there is no active push notification when a NEW ban happens, so the video script's "follow so you catch the next one" needs to stay read as a social-follow CTA, not an in-app alert claim; (2) "flags what replaced the banned ingredient" (LinkedIn script) is **NOT implemented at all** — no ingredient-substitution/replacement tracking exists anywhere in the codebase, this line should not post until either built or cut; (3) certifications are real but undifferentiated (EWG-style/USDA Organic get the same flat +3 as "Made in USA"); (4) animal-welfare/child-labor flagging IS real and already populated — 36 of 540 logged company issues are labor/welfare-related (cocoa, hazelnut, carnauba wax supply chains, real active litigation) — but coverage across companies hasn't been systematically audited as the product DB scales | Two scripts made claims ahead of what the app can actually do; better to catch this before they post than after | Findings written into priorities.md P1 (claims needing a rewrite before posting) and P2/backlog (real feature gaps: active ban alerts, replacement tracking, cert taxonomy, issue-coverage audit) — nothing posted yet, no marketing copy edited without the founder's sign-off on the reword |
|---|---|---|---|
| 2026-07-08 | `marketing/` folder consolidated — founder-directed: keep one folder per calendar month, but make each month complete with **one file per social type** (x, linkedin, video, reddit, calendar). Reconciles with (does NOT reverse) the 2026-07-05 monthly-folder decision. Executed: `month-01` loose files → `2026-07/` (x-2026-07, video-2026-07, reddit-2026-07, calendar-2026-07; linkedin/x-buildstory/launch already there), `month-02/` batch → `2026-08/` (reddit + calendar renamed; tiktok+runway merged → video-2026-08; founder-posts → seeded linkedin-2026-08; x-posts + comparison + transparency folded as sections into x-2026-08). The root `linkedin-2026-07.md` (undated Posts 1–23) was a strict subset of `2026-07/linkedin-2026-07.md` (dated Posts 1–29) → retired. Shared voice exemplars + strategy docs (x-posts-growth-batch, content-framework, ai-video-style-guide, video-scripts-batch, MARKETING-PLAN, mascot-strategy, making_shelf_expose, content-audit) stay at `marketing/` root. Nothing hard-deleted — every merged/superseded original preserved in `marketing/_archive/2026-reorg/` | Founder: "clean up marketing files… all x posts together, all linkedin and video together," then clarified "all social types in each month — july has a linkedin file and x file and video scripts, same for august." Chose x/linkedin/video/reddit+calendar granularity, month-01→July / month-02→Aug mapping | Lossless verified (28 pre-reorg files all accounted for: 6 renamed, 6 merged w/ originals archived, 1 superseded-dup archived, 15 kept). `marketing/` is gitignored so the moves aren't in git, but the entangled DOCTRINE pointers WERE tracked and are fixed same-commit: the dead `marketing/linkedin_posts.md` reference (never existed post-rename) → `2026-07/linkedin-2026-07.md` in CLAUDE.md, cicero.md (×2), sops.md, company-map.md, notification-copy-bank.md; `x-posts-month-01.md` → `2026-07/x-2026-07.md` in cicero.md; cicero.md working-style + content-sprint SKILL.md updated from the `month-0N` example to the dated per-type convention (`video-YYYY-MM` not `scripts-YYYY-MM`, exemplars-live-at-root note). Dated historical records (this log's 2026-07-05 entry, the 2026-07-05 retrospective) left as-is. Founder can delete `_archive/` once satisfied |
| 2026-07-08 | Sentry ANR fix — `src/utils/scorer.js` index build made LAZY. Diagnosis (Fable, from a redacted-stack Sentry "App Hanging ≥2000ms" / REACT-NATIVE-3): scorer.js built its exact/token ingredient indexes in a top-level IIFE that ran synchronously the instant the module was imported, doing regex-heavy `normalize()`/`significantTokens()` work across the ~3k-entry `CACHED_INGREDIENT_ANALYSIS` + `INGREDIENT_DB`. `AppNavigator` statically imports `HomeScreen` (+ other screens) which import scorer.js only for cheap helpers (gradeToColor, etc.), so that whole build ran on the JS thread during COLD START — a removable synchronous cost on the launch path. Fix: converted the IIFE to a memoized `ensureIndexes()` (guarded by `_indexesBuilt`), called at the top of `lookupExactWithSingulars` and `lookupIngredient`, so the index is built on the first real ingredient lookup (when a product is actually scored) instead of at import. In the Hermes release build the module "parse" is cheap bytecode; the real main-thread work was the IIFE executing, which is exactly what's now deferred | Founder: "fix the sentry error." The IIFE was the clearest removable synchronous JS-thread cost on cold start among the launch-path work (DB copy is native/async, data modules are precompiled bytecode). Behavior-preserving refactor of the highest-stakes file, so the 789-test suite is the authoritative check | Committed + pushed as `785baf0` (founder authorized Claude to commit app code directly — see [[feedback-commit-freely-no-build-prompts]] — builds stay the founder's, on their cadence). Every index reader is transitively gated by `ensureIndexes()`; Babel parse clean; **789/789 tests pass** (full scorer characterization suite scores real products end-to-end → identical grades/flags, proving the lazy build is behavior-identical). CAVEAT: the Sentry frames were redacted, so this targets the strongest lead, not a stack-confirmed frame — actual ANR reduction can only be confirmed on-device after a build. If the hang recurs, next step is the symbolicated stack + breadcrumbs (cold vs warm, device, screen). Optional follow-up not done: an idle warm-up (InteractionManager) so the first *scan* doesn't pay the build either — deferred because the build alone should stay under 2000ms off the cold-start stack |
| 2026-07-08 | Marketing-site (fierce-pine-854.higgsfield.app) founder-directed revision pass, deployed live (commits `b9bb925` → `1e61d4e` → `e5deb08`): fixed a headline word-spacing bug (animated `HeadlineReveal` put inter-word spaces INSIDE `inline-block` word spans, where CSS collapses trailing whitespace → "Thetruthbehindyourfood."; moved spaces to text nodes between spans); hero "truth" set italic + drop-shadow via new `emphasisWord` prop; CTA "Show me what's in it" → "Explore the Truth"; three-steps section redesigned into a detective evidence board (staggered paper cards, red pushpins, an S-curve red string layered OVER the cards and tied at each pin) with cutout images enlarged ~64px → 144/176px; §5 transition headline "Case closed. Now you get to walk away smarter." → "Now you see everything." (eyebrow "The breakthrough" removed); PepsiCo money section consolidated (revenue + $3.9M lobbying + $2.1M donations + 44R/56D split into one dossier card, brands to their own card). **PepsiCo lobbying card reframed on founder steer** — stopped defining what the FDA / Congress health committees ARE and instead states the influence ("Lobbies the agency on which additives stay legal…"), and the state-legislature line trimmed to a neutral fact ("Fought proposed soda taxes in cities and states") dropping the CSPI/front-groups editorializing | Founder review of the live site: headers visually broken, wanted the detective theme pushed harder (pins + red string, bigger photos), the flagship company card to lead with lobbying INFLUENCE not brand trivia, and — explicitly — corporate opposition to soda taxes NOT framed as a villain move (his view: soda taxes are government overreach, fighting them isn't a real flag) | All changes verified in a local production build (desktop + mobile, no horizontal overflow, string hidden on mobile stack) and confirmed serving on the live site over HTTP. Every PepsiCo figure/claim stays grounded in canonical `companies.js`; no FDA revolving-door claim invented (data doesn't support one). Deploy was blocked ~25 min mid-session by a Higgsfield git-host 503 outage (auth + deploy API stayed up); a bounded background push-retry landed it once their host recovered. See [[feedback-neutral-regulatory-framing]]. Open: founder to pick a final §5 headline from 5 options (shipped #1 "Now you see everything." as the reversible default) |
| 2026-07-06 | **APPROVED — Shelf Exposé live on the App Store** (https://apps.apple.com/app/id6776718186, verified live: free + IAP $7.99/$49.99 matching config, Health & Fitness, 13+). The approved build carried the 5.6.3 rating-prompt fix, 2.1(a) Google Sign-In fix (Supabase OAuth secret + exchangeCodeForSession code-extraction bug), password-reset deep-link flow, SQLite catalog migration, Sentry, and the onboarding image upgrade | Closes the rejection era: build 26 (IAP/icon/terms) → 27 (privacy label) → 28 (5.6.3 + Google sign-in) → approved 2026-07-06. Preflight-skill discipline + root-cause fixes over guesswork | P0 flips to launch burst + revenue (priorities.md updated same session). Immediate follow-ons: launch-day Cicero burst; marketing site still says "Coming to the App Store" and needs the real store link (both Netlify legal-page site if applicable and the new Higgsfield site's Reward/Waitlist sections); analytics SQL + privacy-label additions ride the next build |
| 2026-07-06 | Password reset failed AGAIN on the founder's real-build test (link reopened the app, no reset screen) despite the 2026-07-06 deep-link fix below. Re-diagnosis: that fix depended entirely on supabase-js firing `PASSWORD_RECOVERY` after `exchangeCodeForSession` — which only happens if the PKCE code-verifier in SecureStore still carries its `/recovery` suffix (verified in installed `@supabase/auth-js@2.106.2` source: verifier stored as `<verifier>/recovery`, suffix checked at exchange, event silently downgraded to `SIGNED_IN` if any other PKCE flow overwrote it) — and exchange failures (expired/pre-scanned link) only `console.warn`ed, invisible in production. Both failure modes produce exactly "link just reopens the app." New approach: stop trusting the event chain — recovery links now redirect to a DEDICATED `healthychoices://reset-password` path so the URL itself identifies the flow; on that path a successful exchange flips `passwordRecovery` directly (event listener kept as backup), failures/URL-embedded errors (`otp_expired`) show a user-visible alert, and a `useRef` dedup guard prevents cold-start double-delivery (getInitialURL + url event) from re-exchanging a used code into a spurious alert. Degrades gracefully: if the founder forgets the allow-list step, Supabase falls back to Site URL and the old event path still catches the happy case | Silent-failure design meant the founder burned a metered build discovering the bug; URL-driven routing has no hidden preconditions and every failure now tells the user what to do | Hadrian built, Fable reviewed the diff line-by-line; Babel-parse clean, 784/784 tests pass. DONE 2026-07-06: founder added `healthychoices://reset-password` to the Supabase redirect allow-list; committed + pushed as d5249ae. Remaining: real-device retest on the next build (also confirm the earlier failed test build actually contained ce48e88 — if not, the founder may have been testing the pre-fix binary) |
| 2026-07-06 | Marketing site design pivoted (rev 2) on founder direction after seeing rev-1 boards: TWO-ACT narrative — dark noir detective investigation (evidence board, paper cutouts, red string, case dossier) that literally breaks open into a bright feel-good act (premium whole foods, brand green on paper-white). Choice-gate hero kept ("Keep believing the label" vs "Show me what's in it") but ALL red-pill/blue-pill imagery and Matrix language dropped: (a) WB IP risk, (b) "red pill" internet-culture baggage, and decisively (c) the app itself displays red/blue partisan donation bars, so red-vs-blue pills would read as "pick your party" on a deliberately nonpartisan transparency brand. Founder's own fork-stab animation (produced externally) gets a designed 16:9 drop-in slot at the act transition; scroll-driven light reveal ships as the working default. Also caught during content prep: TWO shipped app data-accuracy bugs flagged as background-task chips — (1) ingredientCache claims FDA banned Red 40 in Jan 2025 (that was Red No. 3), (2) onboarding's PepsiCo spotlight ($6.2M, 58R/42D) contradicts canonical companies.js ($3.9M, 44R/56D). Website uses canonical companies.js figures | Founder walked through his Pinterest-derived vision (Matrix choice moment, detective collage, two detectives pointing, fork-stab table-break into bright food); Fable translated to spec, flagged the pill risks, founder chose to drop the pill entirely | Rev-2 brief + 6 new boards generated and approved (0 re-rolls), full asset kit generated + optimized (~99 credits total, 144 left). Hadrian built the complete static site (all 8 sections, verbatim-ported privacy/terms routes, D1 waitlist backend, bespoke CTAs) but hit his session limit before reporting — Fable verified the work directly: typecheck/build/qa:fill all pass, review-rubric greps clean, real data verified in place. **COMPLETE 2026-07-07: full site live at https://fierce-pine-854.higgsfield.app.** Founder produced the fork-transition video in Higgsfield (pinned-ends technique: fork-raised start still + white-world end still + one continuous prompt — after two failed free-form attempts, pinning both frames is what fixed the double-headed fork and forced the camera descent); Fable extracted 101 scrub frames (bundled ffmpeg via imageio-ffmpeg — no system ffmpeg on this machine), verified frames individually; Hadrian built the motion pass (pinned canvas scrub 280vh desktop/150vh mobile, Lenis+GSAP bridge, mount-fired headline reveals, cutout parallax, full reduced-motion fallbacks, SSR-safe), Fable reviewed ForkScrub line-by-line and approved. Post-launch copy swap shipped in the same deploy: "Coming to the App Store" → real Download CTA (apps.apple.com/app/id6776718186), waitlist repurposed as post-launch email list ("new case files"), stale "email you at launch" success copy fixed. All gates re-ran clean; deploy verified serving new content + frames over HTTP 200. Remaining: founder scroll-through review, then his DNS cutover call for shelfexpose.app (Netlify keeps serving until he says switch) |
| 2026-07-06 | `shelfexpose.app` connected and confirmed live: Cloudflare DNS (registrar) → CNAME-flattened apex + www records, both DNS-only (gray cloud, not proxied through Cloudflare) → Netlify, which auto-provisioned SSL. Verified via direct `nslookup` against 1.1.1.1/8.8.8.8 and `curl --resolve` (bypassing a stale DNS cache in the assistant's own fetch tool that briefly showed false ENOTFOUND errors) — HTTP 200, valid cert, real page content served | Founder bought the domain and wanted it live; this was step one of backlog item #3 (marketing website overhaul) regardless of which build approach follows | Live. `shelfexpose.netlify.app` still works too, so no existing legal links (app metadata, in-app Terms/Privacy) need to change yet |
| 2026-07-06 | Onboarding value screens upgraded from emoji/letter placeholders to real BUNDLED images (12 product studio shots, all 16 store logos, PepsiCo mark — 1.2MB in `assets/onboarding/`). Key call: bundle local assets instead of hotlinking remote URLs, because onboarding is the one screen guaranteed to run on a fresh install with possibly bad network — remote images popping in late/blank would look worse than the emojis did. Sourcing pipeline was script-first per doctrine (zero agent cost for fetching): catalog image URLs → UPCitemdb keyword search (retailer studio CDN shots; throttled, needs ~16s gaps on trial API) → Wikipedia media-list + Special:FilePath for brand logos. Clearbit's free logo API is DEAD (post-HubSpot acquisition) — don't reach for it again. Every image individually eyeballed by Fable before acceptance; rejected first-pass OFF user photos (blurry bedroom shots), an Aldi NORD logo (US stores are Aldi SÜD — Nord owns Trader Joe's), a 1983 Sam's Club logo, and a 1968 Target logo. Scripts kept at `scripts/fetch-onboarding-assets.js` + `scripts/fetch-onboarding-target-images.js`; provenance in `assets/onboarding/manifest.json` | Founder: first-impression screens "look like crap" with emojis and letter tiles; every product/category needs its real product photo and companies need real logos. Fable planned + sourced/reviewed images, Hadrian built the UI changes | Shipped to working tree: new `src/data/onboardingAssets.js` (29 static requires), ProductBadCard renders real photos in 72px white tiles (emoji fallback kept), PepsiCo mark replaces the "P" tile, `StoreLogo` resolves local→remote→letter so all 16 stores show real logos in BOTH onboarding and Profile. Fable-reviewed the diff; 196/196 tests, parses clean, assets confirmed not excluded by .easignore/.gitignore. Residual risk (named): actual on-device look of varied aspect-ratio photos inside the fixed 72px `contain` tiles is unverified until the next build — check during TestFlight pass alongside the auth-fix verification. Rides the same build as the auth fixes |
| 2026-07-06 | Pre-build audit for the 5.6.3/2.1(a) resubmission found `web/privacy.html` (live, deployed, git-tracked despite `web/` being gitignored — that only blocks new stray files, not the 6 already-tracked pages) still said "we do not use any third-party analytics or crash reporting services," while Sentry was already committed to `main` (`6c2eadf`) and unshipped — a direct contradiction that would go live the moment this build ships | Found during Phase 0 of the submission-preflight checklist; shipping crash reporting while the public policy denies it is a rejection risk and, regardless of Apple, just inaccurate | Rewrote Section 5 to honestly disclose Sentry (error + stack trace only, no identity/IP/device ID — matches `sentry.js`'s actual config), bumped the last-updated date to 2026-07-06. Needs to be committed + pushed so Netlify redeploys before/with this build. Founder still needs to check the App Store Connect privacy label for "Crash Data — Not Linked to You" |
| 2026-07-06 | Build scope: ship everything currently uncommitted together — auth fixes (Google sign-in, password reset, existing-account signup message) + the in-progress SQLite product-catalog migration (new native modules `expo-sqlite`/`expo-asset`, ~10 screens, 161MB db) + notifications/Sentry/buy-this-instead/product-requests from `6c2eadf` — rather than trying to scope down to just the rejection fixes | Founder chose this after being shown the alternative (manually stash unrelated files before building, submit narrower, repeat later) — most of the bundled work was already tested/reviewed in earlier sessions per this log, just never committed; separating it was real effort for uncertain benefit | Founder to commit everything and run the build; confirmed `assets/db/products.db` (161MB, gitignored from git but NOT excluded by `.easignore`, which takes precedence for EAS uploads) will be included in the build archive without needing to be committed to git — avoids the GitHub 100MB file-size limit entirely |
| 2026-07-06 | Signing up with an already-registered, confirmed email silently said "Check your email" with no email actually sent (Supabase's API deliberately hides this to prevent account enumeration — returns 200 + an obfuscated user object with `identities: []` instead of an error). Chose to tell the signer-upper directly ("Account already exists, try signing in") rather than keep the generic message or only log it for the founder | Founder wants the person hitting this to know what happened instead of waiting on an email that'll never come; accepted trade-off: anyone can now learn whether a given email has an account by trying to sign up with it (minor enumeration exposure) — acceptable for a consumer food-scanning app, founder chose this explicitly over the log-only or both options | `AuthScreen.js`'s `handleEmailAuth` now checks `data.user.identities.length === 0` (the documented Supabase signal for this exact case) and shows a distinct alert instead of the generic confirmation message. 196/196 tests pass |
| 2026-07-06 | Password-reset email link opened the app but landed on the main screen with no way to set a new password — a genuinely missing feature, not a bug. Root cause: `resetPasswordForEmail`'s link returns via a real OS deep link (user taps it in Mail), unlike Google sign-in which is caught inline by `WebBrowser.openAuthSessionAsync`. Nothing in the app listened for incoming URLs at all — no `Linking` handler existed anywhere, so the PKCE `code` in the recovery link was never exchanged and `PASSWORD_RECOVERY` never fired | Same PKCE code-exchange mechanism as the Google sign-in bug, just arriving through a different transport (OS deep link vs. in-app browser session) — found while investigating the founder's account-deletion/recreation test pass | Built: `AuthContext.js` now runs `Linking.getInitialURL()` + `Linking.addEventListener('url', ...)` and exchanges any incoming `code` param; tracks a `passwordRecovery` flag off the `PASSWORD_RECOVERY` auth event. New `ResetPasswordScreen.js` (new password + confirm, calls `updateUser`). `AppNavigator.js` now renders it instead of the main app whenever `passwordRecovery` is true, since a recovery session makes `user` truthy and would otherwise silently fall through to the normal main-app branch. `AuthScreen.js`'s `handleForgotPassword` now passes an explicit `redirectTo` instead of relying on the dashboard's Site URL default. 196/196 tests still pass; needs a real-device retest (request reset email → tap link → confirm the new screen appears) |
| 2026-07-06 | Rejection (likely build 28): Guideline 5.6.3 (rating prompt on first launch/onboarding) + Guideline 2.1(a) (Google Sign-In error, iPad Air M3/iPadOS 26.5.2). Audited: 5.6.3 was ALREADY fixed and committed (`720997b`, 2026-07-04) before this rejection landed — no onboarding review-prompt call exists in current tree, confirmed by grep; system-sheet-only on 3rd scan / post-purchase. No code action needed, just confirm it rides the next build. 2.1(a) turned out to be BOTH a dashboard misconfig AND a real code bug, found in sequence: (1) Supabase's Google provider was enabled with a Client ID but no Client Secret (`"Unsupported provider: missing OAuth secret"`) — founder had never set up a Google Cloud Console OAuth client at all, the "Client IDs" field held the iOS bundle identifier instead of a real Google client ID. Fixed by creating a **Web application** type OAuth client in Google Cloud Console (needed for the secret) with `https://huvxeaegygaeotomdqpc.supabase.co/auth/v1/callback` as its authorized redirect URI, then pasting real Client ID + Secret into Supabase. (2) After that fix, flow still failed every time, even fresh install, with `"invalid flow state, no valid flow state found"` (a Supabase Auth **server** error, not a local storage/race issue as first suspected). Root cause confirmed by reading the installed `@supabase/auth-js@2.106.2` source directly: `exchangeCodeForSession(authCode)` sends whatever it's given straight through as the raw `auth_code` POST field — **zero URL parsing**. [AuthScreen.js:150](../src/screens/AuthScreen.js) was passing `result.url` (the entire `healthychoices://auth/callback?code=...` deep link) instead of the extracted `code` param, so Supabase's server always got a garbage `auth_code` matching no flow record | Rejection playbook (`submission-preflight` skill) says check for a no-binary-change path first — but this one needed a binary change once the dashboard fix didn't resolve it. Google Sign-In button was also missing the `disabled={loading}` guard the email button has, made double-tap-triggered duplicate flows possible (secondary hardening, not the root cause) | Both fixed: Supabase provider config (Client ID/Secret) + `AuthScreen.js` now extracts `code`/`error` from the callback URL's query params before calling `exchangeCodeForSession(code)`, and surfaces any `error`/`error_description` from Google/Supabase directly instead of blindly attempting an exchange. Google button now disables + spinners while loading. Full test suite (196/196) still green. Needs a real device retest to confirm end-to-end, then rides the next build |
| 2026-07-06 | Ingredient-elimination campaign gets a stopping rule: stop running waves once a single wave's coverage gain drops below 0.05 percentage points. Waves 1-3 (2026-07-05) already show a clearly diminishing curve: +0.80pp → +0.21pp → +0.11pp | `ingredientCache.js` (711KB, 3,095 entries as of Wave 3) is a static import in `scorer.js` — eagerly bundled into the app, same category of risk as the 212MB products file that needed the SQLite re-architecture, just far smaller today. Flat per-wave file-size cost against shrinking per-wave coverage gain is real bloat risk if run indefinitely to zero | Rule recorded in `context/backlog.md` item #1. Check the coverage delta after each wave before deciding to run the next one. If the campaign gets picked back up after hitting the cap and keeps mattering, the real long-term fix is moving ingredient risk data into the existing SQLite store rather than growing the eager JS cache forever — not urgent now |

| Date | Decision | Why | Outcome |
|---|---|---|---|
| 2026-07-05 | Ingredient-elimination Wave 3 shipped: routing changed from the manual paste-bridge to running Chad's work (gpt-5.5) DIRECTLY via `codex exec` on the CLI — no founder copy/paste needed. Classified 128 of the next top-150 unrecognized ingredients (22 correctly punted), merged into `ingredientCache.js`. Same dual-review pipeline as Waves 1-2 (Hadrian mechanical + independent Opus accuracy) applied to CLI-produced output, confirming no quality drift from the routing change. One fix applied: `'epg'` overstated FDA approval status ("FDA has allowed specific food uses" → softened to accurate hedged framing, it's petitioned not broadly authorized). Also fixed a stale hardcoded path in `instructions/efficiency.md`: the documented Codex CLI binary location had moved (auto-updater changes the hash directory) — doctrine now says to search for it rather than hardcode | Founder asked whether the CLI could prompt Chad directly instead of requiring manual paste each wave; `efficiency.md` already documented this as the "primary" lane, just hadn't been exercised in this campaign yet | 196/196 tests passing, verified independently. **Coverage: 99.11% → 99.22% matched, unknown occurrences 26,734 → 23,213.** Exclude-list fix from Wave 2 confirmed still holding on a second regeneration (zero junk fragments in Wave 4's fresh input list). Direct CLI lane now the standing default for future waves; paste-bridge stays available for anything that doesn't fit a self-contained prompt. Nothing committed |

| Date | Decision | Why | Outcome |
|---|---|---|---|
| 2026-07-05 | Marketing folder reorganized to one folder per calendar month (`marketing/2026-07/`, `marketing/2026-08/`) instead of flat date-named files in the root. Cicero's doctrine (`.claude/agents/cicero.md`) updated to match. Old `marketing/month-01`/`month-02` flat files and folder left untouched as legacy — explicitly the batch-numbering pattern the dates-not-numbers rule was created to replace, not a pattern to continue | Founder wanted monthly content genuinely grouped together; reusing "month 2" numbering would have reintroduced the exact ambiguity the July file's own header already warned about ("the numbering-only scheme became unusable across merges") | `marketing/2026-07/` holds linkedin-2026-07.md + x-buildstory-2026-07.md; `marketing/2026-08/` holds x-2026-08.md. marketing/ is gitignored so this was a pure filesystem move, no git history impact |
| 2026-07-05 | Ingredient-elimination Wave 2 shipped: Chad classified 129 of the next top-150 unrecognized ingredients (21 correctly punted). Merged into `ingredientCache.js`. Independent Opus review found the batch consistent with Wave 1 and the existing cache (dye severities, salt/sugar/flavor families all match), one cosmetic wording fix applied ("refinery syrup" now correctly notes it's likely a label variant of "refiners syrup"). Also fixed the root cause of a problem both Wave 1 and Wave 2 flagged: the same ~8 OCR-noise/numeric fragments (`slat`, bare numbers, `ntss 31`) were resurfacing at the top of every regenerated unknowns list and consuming a real batch slot each time despite being already-confirmed unclassifiable — added a permanent exclude list to `scripts/ingredient-coverage.js` so Wave 3+ don't re-waste effort re-confirming the same non-answers | Two independent reviewers (Hadrian mechanically, Opus on accuracy) flagged the same resurfacing-junk issue across two consecutive waves — real, proven waste worth fixing rather than letting it happen a third time | 196/196 tests passing, verified independently. **Coverage: 98.90% → 99.11% matched, unknown occurrences 33,063 → 26,734.** Wave 3's freshly-regenerated input list verified clean of the previously-confirmed junk (new top entries are genuinely unclassified fragments: "usa", "red", "granular", "refined", "concentrate", etc.). Nothing committed |

| Date | Decision | Why | Outcome |
|---|---|---|---|
| 2026-07-05 | Ingredient-elimination Wave 1 shipped: Chad classified 143 of the top-150 unrecognized ingredients (7 correctly punted as unclassifiable OCR/numeric noise), merged into `ingredientCache.js` (2,695 → 2,838 keys) after independent mechanical validation (Hadrian: parse/keys/enum/merge/tests) + independent accuracy review (Opus: no fabrication, correct CFR citations, Red 40-fragment severity confirmed equivalent to the canonical dye fix) | First real run of the recurring campaign (backlog #1); dual-review (mechanical + accuracy) mirrors the project's standing creator≠reviewer discipline, now applied to Chad's output same as any agent's | 196/196 tests passing, verified independently twice. **Coverage: 98.10% → 98.90% matched, unknown occurrences roughly halved (56,744 → 33,063).** One architectural finding for later: cache-sourced severe ingredients don't count toward the UPF processing-ceiling the way DB entries do (same point penalty either way, just skips the ceiling clamp) — founder decision, not a defect. `remaining-unknowns.json` auto-regenerated, ready to seed Wave 2. Nothing committed |
| 2026-07-05 | August X content batch (main product-fact pillar) drafted by Cicero, reviewed by an independent Opus pass plus direct verification of the highest-risk claims — 20 posts (not the ~25 target; Cicero stopped rather than pad with weaker/repeated claims, per its own accuracy rail). Two real errors caught and fixed: Post 1 cited Kraft Mac & Cheese as still containing dyes (Kraft removed Yellow 5/6 in Jan 2016, and it never had Red 40); Posts 10 & 16 overstated West Virginia's dye ban as an active statewide retail restriction (only the school-meals piece is active — the retail ban was blocked by a federal judge in Dec 2025 before its Jan 2028 start date ever arrived). Also corrected: carrageenan NOSB vote was 10-3 not "two-thirds," USDA overrule was 2018 not 2016 | Never let the writer self-certify — mirrors the content-sprint pipeline's mandatory separate-evaluator discipline, applied here since this ad-hoc batch didn't go through that skill | File: `marketing/x-2026-08.md`. All fixes applied, file's own review section updated to GO status. 10 other specific regulatory/scientific claims independently verified accurate as originally drafted (BHA Feb 2026 FDA reassessment, sodium phosphate, sulfites/FDA labeling threshold, propylene glycol, EU aluminum date, celery-powder nitrates, PGPR/castor oil, FDA benzene testing, palm oil labeling). Nothing scheduled, nothing committed |

| Date | Decision | Why | Outcome |
|---|---|---|---|
| 2026-07-05 | Mascot strategy/vibe doc added (`marketing/mascot-strategy.md`) — summarizes a founder-sourced article (Duolingo owl, Discord Wumpus, GitHub Octocat, Reddit Snoo, Finch, YAZIO case studies: mascots need a real in-product job + 5-10 emotional states + one locked reference, not just marketing appearances) and ties it directly to the same-day mascot reframing in `high-impact-plan-2026-07-05.md` §4 — validates the in-app-companion-first, sticker-overlay-second direction rather than changing it | Founder found real external validation for the plan the same day it was written; wanted it captured for Cicero's future vibe/tone reference | Doc created; visual design/inspiration sourcing still pending, founder's call separately |
| 2026-07-05 | High-impact backlog planned (all 4 items) at `context/high-impact-plan-2026-07-05.md`; two ready-to-paste Chad briefs on the task board (ingredient-unknowns wave 01 — standing/recurring; shopping-guides research). Key routing: Chad = both research lanes (per doctrine — Haiku never generates facts, Sonnet builds); guides build = Hadrian after Opus reviews the research; website = general-purpose Sonnet (NOT Hadrian — not RN app code); mascot REFRAMED post-video-pivot as in-app companion + video sticker (not a synthetic-video actor — that lane died with Runway today), sequenced last. Grounding corrections found: `web/index.html` is already a live deployed landing page (backlog said legal-pages-only) so the website is an upgrade, not an overhaul; no waitlist table exists in Supabase (plan specifies `waitlist_signups` + insert-only RLS mirroring product_requests); ingredient campaign tooling ALL exists already (`ingredient-coverage.js` enumerator, `merge-ingredient-batches.js`, batch format) — regenerated the stale 2026-06-18 unknowns list same session: app-real match rate is 98.10%, 56,744 unknown occurrences remain, top-150 (~33% of them) = wave 01 scope, mostly label-fragment phrases not exotic chemicals | The 4 high-impact items needed a real plan with explicit model routing before the Opus handoff (Jul 7) and the Aug 3 founder-availability cliff; founder asked for Chad to carry the research load and for the video pivot to be folded into the mascot/website thinking | Plan + briefs written, nothing built, nothing committed; founder decisions pending: paste wave-01 brief, paste guides brief, green-light website Phase 1 (waitlist pre-approval), accept mascot sequencing |
| 2026-07-05 | Video strategy REOPENED: the locked "synthetic AI actors only, no founder face" call (2026-06) is superseded — founder will film real video himself, already recording. Synthetic cast's future role TBD (not confirmed dropped). The DoDD 1344.10/endorsement consideration that motivated "no founder face" originally doesn't go away — it now applies to what's literally in frame (uniform/rank/setting), same test as the existing military-mention rule, just visual instead of textual | Founder's own hands-on experience: Runway wasn't working out in practice | Memory updated (`marketing-plan.md`); Fable planning pass (same session) to fold this into the high-impact backlog plan, especially the mascot item's role alongside real founder video |
| 2026-07-05 | Backlog cleanup: removed 4 stale entries that were actually already shipped (notifications v1, buy-this-instead, app-feel polish pack, pantry report card) — found while building an impact/cost table of the full backlog and verifying each item's real code status rather than trusting the file. Added 2 founder-flagged items: marketing website (`shelfexpose.app` domain + Netlify already live, serving only legal pages — the real landing/waitlist site is unbuilt) and a mascot for marketing video (extends the locked synthetic-AI-actor cast, not a replacement for the no-founder-face rule) | backlog.md hadn't been updated since the 2026-07-04 "ship app-feel polish, notifications, buy-this-instead..." commit; stale entries would have wasted a future session re-discovering already-done work | backlog.md now reflects only genuinely open items; both new items sized as overhaul/feature (schedule deliberately, not batch-work) |
| 2026-07-05 | RLS locked down on `product_requests` (`supabase/product_requests_setup.sql`) — insert-only for anon/authenticated clients, no select/update/delete, mirroring the scan_events pattern. RevenueCat Android key explicitly deferred (no Android release planned); clarified the existing iOS RC key being visible in git is fine — it's a public SDK key, not a secret, unlike the still-missing `google-service-account.json` EAS references | Table existed with no RLS (ad-hoc dashboard creation, no tracked migration); app only ever inserts to it (My Requests reads local AsyncStorage), so client read access was never needed | **Founder ran the SQL, confirmed working.** Table now closed to client reads, matching scan_events' posture |
| 2026-07-05 | Opus-handoff package shipped (Fable's final manager act): evidence-based retrospective at `context/opus-handoff-retrospective-2026-07-05.md`; ONE new skill `submission-preflight` (App Store checklist pipeline — the only repeated multi-step workflow not already a skill; builds 26/27 rejections were all checklist misses); `instructions/founder-usage-guide.md` (how Christian briefs the system, real if-you-say-X examples); `instructions/delegation-templates.md` (the 4 brief shapes that worked); efficiency.md Handoff note expanded in place with the hard-won judgment calls (calibration failure modes, verify-independently, dup-JS-key trap, founder-communication rules, open items). Deliberately NOT built: skills for test-expansion (delegation-shaped → template instead), Chad briefs (already templated), ingredient-cache campaign (hasn't run once — pilot before skill-ifying) | Manager hands to Opus 2026-07-07; the context/memory infrastructure already worked — this audit filled the one skill gap, extracted templates, and wrote down what lived only in Fable's head | Docs-only, no app code touched, nothing committed (founder commits) |
| 2026-07-05 | Wave 3 fully closed out: last two `PRODUCT_DB` consumers migrated — `alternatives.js` ("buy this instead") and `productRequests.js` (My Requests). New `productStore.getCuratedGradeABCandidates()` added (SQL does the curated+grade-A/B cut using precomputed `grade`, replacing per-candidate client-side `scoreProduct()` calls entirely); category-normalization regex logic deliberately kept in JS rather than reimplemented in SQL (correctness risk for no benefit) | Close out the full re-architecture — every screen/util now off the 212MB eager JSON except `products.js` itself (the build-time source) | 196/196 tests passing (5 suites), verified independently. Real end-to-end proof: Cheerios Original → "Nature's Path Flax Plus Multibran Flakes" (same category, grade A, sorted correctly). `getCuratedGradeABCandidates` measured at 78.69ms/309 rows (no index — one-time per-product-view lookup, not a hot path, not worth optimizing). Final grep sweep: zero live `PRODUCT_DB` imports remain outside `src/data/products.js` itself. **Wave 1-3 complete.** Still open: the EAS-build asset decision (commit ~153MB `products.db` vs. build hook) and the on-device asset-copy flow, both require a real build to resolve, not today's work |
| 2026-07-05 | Wave 2 pilot: `src/data/productStore.js` (async SQLite-backed `getProductByBarcode`, via `expo-sqlite`+`expo-asset`+`expo-file-system/legacy`) built and wired into ScannerScreen's local-lookup fallback ONLY — same behavior (still remote-first), just a new data source. All other 7 screens + `PRODUCT_DB`/`products.js` untouched, deliberately, per founder's "regardless of app approval" push to keep this moving while Fable's still the manager (hands to Opus 2026-07-07) | Founder wants the catalog re-architecture (212MB eager JSON → SQLite) to keep moving independent of the Apple review outcome; pilot-one-screen-first de-risks an 8-screen migration | 161/161 tests passing (new suite: `productStore.test.js`), verified independently. Real-schema Node-side proof against the live 153MB DB confirms correct scoring end-to-end. **Schema gap found + FIXED same day**: Wave 1's table was missing `packaging`/`isOrganic`/`isVegan`/`isGlutenFree` — invisible for 135,599 generated products (none have this data) but real for 38+ of the 892 manual products (packaging penalty, diet badges). Extended schema, regenerated DB (136,491 rows, unchanged), proved fix with a real Birds Eye product (packagingPenalty=9, score=91/A) and a real WestSoy product (organic/vegan/gluten-free badges). **Two items flagged, not solved, founder-decision-pending**: (1) `assets/db/products.db` is gitignored, only exists locally — next real EAS build needs either the file committed (~153MB) or a build hook regenerating it, decide before that build; (2) the actual on-device asset-copy-then-SQLite-open flow is unverified beyond mocks/Node-proof — only a real device build closes that gap |
| 2026-07-05 | Testing expanded to the money/data files: `subscription.js` (RevenueCat) + `storage.js` (AsyncStorage), Hadrian. Suite now 147+ tests/3 files | Founder asked specifically for tests that catch "we don't make money or someone leaves a one-star review" — subscription is the purchase path (site of the past "paid, nothing unlocked" bug), storage is scan history/prefs (data loss = rage reviews) | 147/147 passing, verified independently. 4 fail-open/silent-reset behaviors found — see next row for fix outcome. Also flagged: `purchaseRCPackage` throws on failure (by design, UI must catch) while `restorePurchases` swallows errors and returns `false` — asymmetric, intentional-looking, left as-is (changing either contract risked breaking un-audited call sites). `useProStatus` hook logic left untested — no React-hook testing library installed, not added without asking first |
| 2026-07-05 | All 4 flagged storage.js behaviors FIXED + Sentry (`captureException`) wired into every silent catch across `storage.js` and `subscription.js`, incl. `purchaseRCPackage` (logs genuine failures, explicitly skips user-cancelled, rethrows unchanged) and the `useProStatus` listener catches (added directly, Hadrian's brief scope had missed these two) | Founder: fix what we find, and report everything to Sentry so we know when it happens instead of finding out via a review | **checkAndIncrementDailyScan**: corrupted data now self-heals (treated as fresh state, overwrites the bad entry on first call) instead of granting unlimited scans forever. **addScanToHistory/getScanHistory/getUserPrefs**: fallback behavior unchanged (safe defaults), but now report to Sentry so corruption is visible instead of silent. 147/147 passing, verified independently twice. Founder asked about a fully-autonomous "Sentry finds an error → auto-fixed by Claude" pipeline — **declined to build that**: conflicts with standing rules (agents never commit, builds are founder-approved). Recommended instead: Sentry's native GitHub-issue integration (already installed per founder's screenshot, 1 configuration) — auto-creates a GitHub Issue per new error, human reviews/directs the fix same as any other session. Founder to confirm the GitHub integration's specific config (auto-create rule) next |
| 2026-07-04 | Jest + jest-expo added, 76 characterization tests written for `src/utils/scorer.js` (Hadrian). `npm test` now runs the scoring engine's logic locally, no build needed | scorer.js is the highest-stakes file (silently mis-grades food, nothing crashes to reveal it) and the cheapest fix — devDependency only, zero build/ship risk. First step of the "make it more professional" priority (no tests / monolith screens / no TS / eager-load catalog / no pre-submit QA process were the 5 gaps identified 2026-07-04) | 76/76 passing; real data files used, unmocked. **Immediately found a live bug** (see next row) — proof of concept validated |
| 2026-07-04 | Bug found + FIXED: `src/data/ingredients.js` defined `'red 40'`/`'yellow 5'`/`'yellow 6'` twice each — original entries at `avoid`/high-risk, a later "Phase 2" section redefined all three at milder `caution`. JS silently lets the second object-literal key win, so the stricter originals became dead code. Alias keys (`fd&c red 40`, `allura red`, `tartrazine`) also carried the old milder values, inconsistent with their canonical dyes | Real scans of products containing these dyes (by primary name or alias) were scoring more leniently than the ingredient DB's own intended severity — a correctness bug in the exact "trust our math" mission the app sells | **Fully fixed by Hadrian, two passes**: all three dye duplicates removed (red 40 risk 8/avoid, yellow 5 risk 7/avoid, yellow 6 risk 7/avoid, each defined once); all three alias keys corrected to match their canonical dye; tests updated for the first pair, none needed for yellow 6/aliases (none existed pinning old values); 76/76 passing both passes; verified via Babel parse + `npx jest` + manual grep. **`natural flavors`/`artificial flavors` also resolved** (founder: apply the established rules strictly, don't invent new severity): analyzed against the other 6 flavor-family variants (`natural flavor`, `natural flavoring`, `artificial flavor`, `artificial flavoring`, `natural and/& artificial flavor(s)`) which all consistently used risk 4/`caution` (natural) and risk 5/`caution` (artificial) with an "umbrella term, up to 100 undisclosed chemicals" note. Both duplicate keys' currently-ACTIVE (winning) entries already matched that convention — **no live scoring bug here**, unlike the dyes. Removed the dead, inconsistent first-defined entries (natural flavors was needlessly milder at risk 3/`moderate`; artificial flavors' dead entry had the same risk/flag, just a weaker note) for database hygiene. 76/76 passing, zero behavior change since JS was already resolving to the correct entries. Still open: `singularize()` misses `-es` plurals (e.g. "shellfishes" won't match "shellfish") — low priority, not pursued today. LinkedIn Post 13's same Aug-2024/June-2025 timeline imprecision (see next row) — **founder declined to fix**, reach too small to matter |
| 2026-07-04 | LinkedIn stays on autopilot despite flat engagement (0 reactions/comments, 24-250 impressions across 14 sent posts) — no network-building push, no reallocation. Copy quality isn't the issue (origin-story series reads well); root cause is 1st-degree-network size, which LinkedIn's algorithm gates reach on | Scheduling costs ~5 min for 10 days of posts; not worth spending scarce founder hours chasing a channel that was never the designated growth engine (short-form video is, per [[marketing-plan]]) | Standing; revisit only if the time cost changes or video channel needs the slot |
| 2026-07-04 | Infrastructure: shelfexpose.app domain bought; Resend = Supabase custom SMTP (DNS verified, rate limit raised, pw-reset tested) — fixes the 2/hr built-in sender that would have silently eaten launch signups. Sentry adopted for crash reporting (identity-free config, DSN pending). Rejected from the "standard stack": Stripe (Apple IAP mandatory), Clerk (auth shipped), PostHog (no-tracking conflict), Upstash/Pinecone (no fit) | App requires accounts → verification email is on the signup critical path; crashes currently invisible with metered builds | Resend LIVE; Sentry wiring next |
| 2026-07-04 | Review prompt rebuilt for Guideline 5.6.1: custom Alert pre-prompt REMOVED (Apple disallows custom review prompts), onboarding moment killed, system sheet fires directly on 3rd successful scan or post-purchase, 14-day cooldown | Rejection risk hiding in shipped code; Apple's Apr 2026 guidance reaffirmed system-prompt-only | Built + Fable-reviewed; awaits founder commit, rides next build |
| 2026-07-04 | App-feel pack: haptics expanded (errors, grade-keyed reveal, purchase, selection taps) + grade-reveal spring animation on ProductScore | Score reveal is the emotional payoff; polish there is the cheapest "feels premium" win | Built + Fable-reviewed; awaits founder commit |
| 2026-07-04 | Notifications v1: LOCAL-only (no push server), priming step in onboarding before the once-ever OS prompt, 3-day welcome drip + Saturday-9am weekly (copy rotates on app launch), Profile master+weekly toggles (4.5.4 opt-out), playful no-fact copy in code; verified fact bank at context/notification-copy-bank.md (31 lines, primary-sourced; review caught a stale BVO claim) | Retention lever (backlog #2); local-only = zero infra + zero data collection, consistent with the no-tracking stance; facts stay out of code until founder approves | Built + Fable-reviewed; needs founder commit + next build (new native module) |
| 2026-07-02 | Build 28 submitted (analytics layer + ingredients regroup + store logos + privacy-label fix); Netlify auto-deployed privacy.html via GitHub push despite a failed local CLI link | Fixes the 5.1.2(i) tracking-label rejection; ships the anonymous-analytics groundwork | Awaiting Apple review |
| 2026-07-02 | **NO user tracking, NO PII selling — ever.** Long-term data asset = ANONYMOUS aggregate scan trends (market-research style, CPG buyers); analytics layer built identity-free from day one | Brand IS trust (VISION: "never selling them out"); PII sale = pennies/user + FTC/CCPA exposure + acquisition-diligence liability; aggregate demand signals are the real asset | Standing; analytics layer in build 28 |
| 2026-07-02 | Build 27 rejected (5.1.2i ATT): privacy label had "Used to Track = Yes" on collected types; app has zero tracking SDKs. Fix = flip toggles to No + Resolution Center reply; no binary change | Collection was declared honestly; the separate "tracking" toggle means third-party-ad linkage/broker sale, which we do not do | Reply drafted; founder flipping toggles |
| 2026-07-02 | Military-mention rule refined: biographical context ALLOWED in marketing (Army/AIT/location/scarce-resources story); prohibited = position/rank/uniform as pitch, implied DoD endorsement, implied duty-time building. Test: selling vs. setting the scene | Founder correction — JER restricts endorsement-by-position, not biography | Doctrine + Cicero updated |
| 2026-07-02 | Haiku ban refined: never GENERATES facts (no search-and-report/barcodes/money/legal/user-facing); may classify, extract-from-given-text, reformat — always behind a validator or Sonnet+ review | Barcode fabrication was a confabulation-under-absence failure, not a research failure; gpt-5.5 covers most cheap-bulk anyway | Doctrine updated |
| 2026-07-02 | gpt-5.5 integrated via Codex CLI (`codex exec`, ChatGPT sub) — bulk/mechanical/investigation lane; model ranking table (cost/intelligence/taste) + standing escalation permission adopted | Founder ChatGPT sub = effectively free capacity; Fable budget scarce | Verified working (smoke test 2026-07-02) |
| 2026-07-02 | Efficiency doctrine + model routing (instructions/efficiency.md): manager plans/delegates/reviews only; Sonnet default for sessions; scripts for determinism; Chad task board for bulk work; **manager role hands to Opus Jul 7** | Fable at ~50% weekly budget; grunt work on manager models is pure waste | Active |
| 2026-07-01 | Created `context/`, `memory/`, `instructions/` + CLAUDE.md as the project's operating system | Persist business context across sessions and AIs | Active |
| 2026-07-01 | Cicero agent for all social writing; trained on X + LinkedIn voices; journal pipeline at `marketing/journal/` | One permanent voice identity beats re-briefing every spawn | Created; shakedown pending |
| 2026-07-01 | Octavius: script-first product pipeline (fetch chain OFF→OFF-search→USDA→UPCItemDB, agent for misses only) | Old flow was 4–8 agents/product; unaffordable at 2,000-product scale; single-source (OFF) fragility | ~80% token cut; verified live on 5-product test incl. OFF 503s absorbed |
| 2026-07-01 | Political-analysis pipeline stays multi-agent (NOT collapsed into an agent) | Its value is legal separation; one agent holding 4 adversarial roles recreates the liability | Standing |
| 2026-07-01 | Allergens personalized: never "Bad" universally; zero score penalty; red tag only for the user's own allergens | Peanuts shown as "Bad" to a no-allergy user reads as the app being wrong — fatal for a trust product | Shipped to code; in build 27 |
| 2026-07-01 | Tiered severity colors (red = high-severity only); calm 3-tone home tiles; phone-first (no iPad layout work yet) | Wall-of-red dilutes real warnings; rainbow tiles fought the brand; effort focus | Shipped to code; in build 27 |
| 2026-07-01 | Pricing: $7.99/mo, $49.99/yr, **50% off first year on BOTH plans**; paywall copy driven from live RC introPrice | Match real ASC config; never advertise an offer the store doesn't have | Code + terms page aligned |
| 2026-07-01 | Build 26 rejection root causes: RC entitlement had NO products attached; IAPs never submitted; stock icon (4.3a); Terms link missing from metadata | — | All fixed (new shelf+magnifier icon, RC mapping, ASC steps); build 27 pending |
| 2026-06-30 | Company spotlight = weekly rotation through 30 household-name brands (anchored: Sargento week of 6/29) | Recognizable brand each week; "this week's free unlock" is the freemium hook | Shipped; in build 27 |
| 2026-06-30 | Marketing voice: NAME NAMES. Companies, ingredients, real danger flags. Accuracy is the only rail, not timidity | Anonymized "wellness fog" copy defeats the mission and kills virality | Locked in content-framework.md + Cicero |
| 2026-06-30 | Content ordering law: shock first → 5 checkable products → why → CTA. Video: app mention ONCE at end; 3 hooks per idea; ≤35s | Cold-start algorithm reality: first 2 seconds decide everything | Locked; 20 scripts written to this format. **Checked 2026-07-04**: reviewed live X post performance — misread a reposted third-party viral post (Hosea/Shahzad Khan, 490K views) as one of Christian's own posts. Actual own-content posts (incl. the dye/lipstick post) all sit at ~1 like / 9-20 views — X is flat across the board, no format-driven breakout observed yet. No hook-quality conclusion supported by this data |
| 2026-06-30 | Icon/splash: original shelf + wood-handled magnifier mark (user-iterated: 20% smaller magnifier, wood handle, items seated) | 4.3(a) rejection — old icon was a stock magnifier-eye | Rasterized (opaque, no alpha), committed |
| 2026-06-29 | Home reorder: hero (700+) → recent scans → weekly free unlock (with company logo) → category grid | Spotlight is the conversion hook; belongs above the fold-ish | Shipped; in build 27 |
| 2026-06 | Freemium gate: company transparency data (money trail) is THE Pro feature | Transparency data is the differentiated value worth paying for | Standing; paywall live |
| 2026-06 | 30-day launch goal: $1,000/mo ≈ 40 annual subs; daily short-form video is the growth engine; waitlist before approval | Honest math beats vibes; velocity drives category rank | Plan at marketing/30-day-launch-plan.md |
| 2026-06 | EAS builds are metered — validate via logic checks; user decides when to build | Wasted builds = wasted money | Standing SOP |
| 2026-06 | Haiku banned from all research | Fabricated sequential placeholder barcodes rather than admitting failure | Standing; encoded in Octavius |
| 2026-05/06 | Keep the 135k-product generated catalog (products_generated.json) | It's the scan-anything engine (ingredients+nutrition for scoring), not dead weight | Standing; 212MB eager-load flagged for later re-architecture |
