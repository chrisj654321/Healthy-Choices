# catalog-database

Build, validate, and publish the product database that ships inside the app
(`products.db`) and the remote manifest that tells devices which version to
download. Includes the daily ingestion pipeline (OpenFoodFacts / USDA) and
its Windows Task Scheduler wrappers.
