# ingredients

Ingredient audit tooling: merge reviewed ingredient batches and score-change
proposals into `src/data/ingredients.js` / `ingredientCache.js`, measure
coverage, and plan the next research waves. Absorbed the former
`scripts/ingredient-audit/` folder.
